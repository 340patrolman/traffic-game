# SEOUL PATROL (traffic-game) — 모든 세션이 먼저 읽는 파일

브라우저에서 바로 실행되는 3인칭 교통경찰 순찰 게임. 1순위는 「재미있는 주행」, 그 위에
「위반 차량을 발견해 우측에 정차시켜 단속하는 일」을 얹는다. 교육용으로도 쓰이지만 교육으로 한정하지 않는다.

## 절대 규칙

* 빌드 도구 없음. npm·Vite·번들러 금지. `index.html`을 열면 바로 실행된다. GitHub Pages(main / root)에 그대로 올린다.
* 의존성은 three.js 1개만 `lib/`에 vendored. CDN 로드 금지(오프라인 실행).
* 외부 3D 모델·이미지·오디오 파일 0개. 건물·차량·사람·표지·사운드는 전부 코드로 생성(절차형 지오메트리 + Web Audio 합성).
* 서버 0대. 네트워크 요청 0. 진행 저장은 localStorage만 쓰고 키는 반드시 `tg_` 접두사. `tb_`로 시작하는 키는 읽지도 쓰지도 않는다(같은 origin에 다른 앱이 있다).
* 「GTA」「Grand Theft Auto」 및 그 게임의 지명·차량명·로고·서체·UI 모사 금지. 실존 자동차 브랜드명 금지. 게임 이름은 네가 새로 짓는다.
* 총기·전투·차량 파괴 쇼 없음. 플레이어는 경찰이다. 사람을 치면 큰 감점과 즉시 임무 실패.
* 폰 우선. 세로·가로 모두 대응, 터치 조작이 1급 시민. 키보드·마우스는 부가. 중급 안드로이드 폰에서 30fps를 지키고, 프레임이 떨어지면 교통량·행인·그림자를 자동으로 줄인다(이미 화면에 있는 것은 지우지 않고 새로 스폰하는 양을 줄이는 방식).
* 법령 수치(범칙금·벌점·조문 번호)는 코드에 하드코딩하지 않는다. `data/laws.json` 한 곳에 두고, 확인되지 않은 값은 `"verified": false`로 표시해 화면에 「확인 중」으로 보인다. 네가 기억으로 채운 숫자를 확정값으로 표시하지 마라. 정확한 값은 소유자가 별표 원문으로 대조해 채운다.
* 한 번에 한 층만 고친다. 주행 물리와 단속 로직을 같은 커밋에서 동시에 바꾸지 않는다.
* 작업 끝에는 반드시 직접 실행해 확인하고, 콘솔 에러 0을 확인한 뒤 보고한다.

## 구조 (v0.2 기준)

* 모듈 시스템 없음. 모든 스크립트는 고전 `<script>`로 순서대로 로드되고 전역 `TG` 네임스페이스에 붙는다
  (file:// 로 열어도 실행되어야 하므로 ES module·importmap 금지).
* `js/config.js` 수치 → `js/city.js` 도로망(격자 노드 + `frameAt`: 어디서든 차로 좌표·제한속도·갓길을 주는 단일 창구)
  → `js/world.js` 도심 메시 → `js/terrain.js` 지형 높이 함수·순환 고속도로(ring)·연결로(connE/connN)·램프 4개·강·바다·하늘·나무
  → `js/signals.js` 신호 → `js/vehmesh.js` 차량 실루엣 로프트 → `js/vehicle.js` 플레이어 물리 → `js/traffic.js` AI(격자 기동 S/R/L/X + 링크 경로 + 위반)
  → `js/peds.js` 행인(관절·무단횡단) → `js/enforcement.js` 목격→정차→고지 → `js/hud.js` → `js/input.js` 조이스틱·버튼·키보드
  → `js/perf.js` 밀도 자동 조절 → `js/save.js` → `js/main.js` 루프·규칙·인트로·테스트 훅.
* 좌표 관례: heading h → forward = (sin h, cos h), right = (−fz, fx). 방위 0..3 = 남(+z) 동(+x) 북(−z) 서(−x). 우회전 = (d+3)%4.
* 링크 방향 A = 샘플 증가 방향. 링 A = 안쪽 차로(시계 방향), 접속부(램프)는 A 에만 붙는다(평면 교차 없음). 차로 오프셋은 진행 방향 우측 +.
* 실존 자동차 브랜드·모델명은 쓰지 않는다(소유자 규칙). 순찰차는 「서울경찰 POLICE」「112」 라벨의 백색·청색 띠 도색.
* 검증은 `?test=1` 의 `TG.test.step(초)` 로 rAF 와 무관하게 결정적으로 돌린다(숨은 탭에서도 동작). README 의 훅 목록 참고.
* 도로 폭은 도로마다 다르다(`city.halfV/halfH`, `lanesV/lanesH`; `AVENUE_V/H` 인덱스가 왕복 4차로). 정지선·횡단보도 거리는 `city.stopDist(node,d)`·`crossNear/crossFar`,
  차로 오프셋은 `city.laneOff(axis, idx, laneIdx)`, 갓길은 `city.shoulderOff`, 보도선은 `city.sideOff`. 상수를 직접 쓰지 말고 이 함수들을 쓴다.
* 확성기(PA) 안내는 `TG.audio.pa(text)` — 브라우저 내장 speechSynthesis(오프라인). 음성이 없으면 차임만 울린다. 오디오 파일은 여전히 0개.
* `data/laws.json`은 http(s)로 열었을 때 fetch로 읽는다. file:// 에서는 브라우저가 fetch를 막으므로
  범칙금·벌점 칸이 「확인 중」으로 보인다(값 복제본을 두지 않는다 — 정본은 한 곳).
* `ref/`는 참고용 clone. 커밋·배포에 포함하지 않는다(.gitignore).

## v0.6 추가(2026-09-06)

* `js/weather.js` — 날씨·시간대 5종(맑음·석양·밤·비·눈). 조명·안개·하늘색(`terrain.skyMesh`)·노면/지면 색(`TG.mats.road/ground`)·창문 불빛(`TG.mats.facade` emissive)·입자·가로등 점·전조등(SpotLight)을 한 곳에서. 그립은 `weather.grip` → `player.surfaceFactor`.
* IC 8곳: `terrain.js` 의 `ICS[]`(노드·방향·링 각도·via 점) → `conns[]`(연결로) + `ramps()`(우회전 진입 on / 진출 off). 연결로는 램프 분기점(링 접속점 42m 전)에서 끝난다 — 그 너머 스텁이 링 밑으로 들어가던 버그의 원인이었다. 분기점 너머는 지형이 링 높이로 이어지므로 차단벽을 두지 않는다.
  링 가드레일·다리 난간은 램프가 가로지르는 곳(`rampGap`)에서 비운다. 램프 다리에는 난간 충돌이 없다. `frameAt` 는 램프 옆 본선 위(합류부)에서 본선 프레임을 쓴다.
* 다리 위 `heightAt` 는 노면 높이(강 위에서 빠지지 않음). 물 복귀는 `!frame.onRoad` 일 때만. 빠짐·끼임·지도 밖은 `recoverToRoad`(일시정지 메뉴 「도로로 복귀」).
* 격자 스텁 끝 12곳은 낮은 벽 + 황·흑 차단봉(`city.walls[].stub`, world.js 가 그림).
* 모드(`G.mode`): patrol / free(랩 타임 = 링 인덱스 0 통과) / circuit(`terrain.circuit`, 교통 0, `coachUpdate` 코칭) / study(`js/study.js` 카드, `laws.json.study12`, `G.startScenario(id)`).
* 차량: `vehmesh.js` 단면 로프트(13점 × 정점 법선), 경찰 띠·황색 선은 단면 색 띠. 실내 `interior(T)`·`layout(T)` 는 눈 기준. `vehicle.js` 디지털 계기판·내비(미니맵 캔버스)·MDT(`player.mdtInfo` 는 main 이 채움).
* 캐시: 스크립트 주소 `?v=X`(index.html·sw.js 둘 다) + sw CACHE 이름을 배포 때마다 올린다. GitHub Pages 는 max-age=600 이라 안 올리면 새 HTML 이 옛 JS 를 부른다(키보드 먹통 원인).
* 검증: `_verify_tail.js`(기능 전체) · `_ic_tail.js`(IC 32구간 + 링 자동 조종 주행) · `_probe_tail.js`(도로 끝) · `_shot_tail.js`(장면 스크린샷). `?norender=1` 로 헤드리스에서 빠르게 돈다. 브라우저 페인이 숨겨지면 rAF 가 멈추므로 `step()` 만 믿는다.

## v0.6.3~ 추가(2026-09-06 밤)

* 지도 = 강남·서초 축약(`city.roadNamesV/H`, `hName(j,x)` 로 서초대로|테헤란로, `LANDMARK_BLOCKS`, `inSchoolZone`, `hasStub`). 모든 격자 도로 4차로(`AVENUE_V/H` 전부). 한강은 `riverZ(x)`(북쪽 동서), `hBase` 가 강 둔치를 평탄화. 스텁은 IC 8곳만.
* IC 이름·안내표지: `terrain.js` `IC_INFO`, `gantry(L,i,textA,textB)`(A/B 방향 글 따로), `TG.tex.hwSign('줄1|줄2')`.
* 순찰차 도색은 데칼: `TG.tex.liverySide(flip)/liveryHood()` + `vehmesh.sideDecal/hoodDecal`(DoubleSide 필수, 로프트 띠는 흰색).
* 차내: 눈은 `wsBase − 0.95`, 카메라 near 0.12(핸들·손). 핸들 그룹 `steer3d` 에 손·소매 포함.
* 키보드: `input.js` `codeOf(e)` — `e.code` 가 비면 `e.key`(한글 자모 포함)로 복원. 방향지시등 `Comma/Period`, 주행 모드 `KeyN`, 미니맵 `Equal/Minus`.
* 교통 AI: `car.trait`(phone/litter/animal), `car.signal`·깜빡이, 4차로 차로 변경(`lcShift` 를 `car.extra` 로 반영, `noSignalViolator` → 'nosignal', 정지선 32m 안 → 'solidline'), `lcForce` 는 테스트용. `flag(car,type,node,seen)` 은 목격 없이도 기록(퀴즈 정답 근거).
* 플레이어: `player.signal`, 차로 변경 판정(`checkRules` 8-2, 앞 교차로 정지선 30m 안 = 실선), 감속 안내(8-3, `hud.hintNow`), `driveMode` sport 배율, `windLat`(강풍).
* 날씨 `auto`(기기 시계·달)/`random`/`windy`. 실제 기상 연동은 「네트워크 0」 규칙상 하지 않는다.

## 확인 절차

`index.html`을 브라우저로 연다(파일 또는 정적 서버). 콘솔에 `[TG]` 로그 외 에러가 없어야 한다.
`?stress=1`을 붙이면 교통량 최대 + 프레임 저하 시뮬레이션 로그(`[perf]`)가 찍힌다.
`?test=1`을 붙이면 `TG.test.*` 자동 확인 훅이 활성화된다(README 참고).

## v0.7.2 추가(2026-09-07) — 보행자 모드 · 게임패드

* 모드 `walk`(🚶 보행자 체험): `js/walker.js` `TG.Walker`(도보 경찰관 메시·카메라 기준 이동·목적지 빛기둥) + `TG.walkerPlace(city, signals, x, z)`(보도/횡단보도/차도/교차로 판정).
  main.js `startWalk/walkRules/walkCamera/walkUpdate` — 순찰차는 강남대로 갓길에 세워 두고 걷는다. `traffic.player`·`peds.player`·`peds.walker` 를 walker 로 바꿔 차량 AI 가 보행자로 보고 선다
  (walker 는 `vF`·`len`·`telemetry.speed` 등 차량 AI 가 읽는 필드를 갖는다). 규칙: 적색 보행 신호에 횡단보도 진입 = `walkRed`(제5조), 횡단보도 밖 차도 = `jaywalk`(제10조), 녹색 진입 후 반대편 보도 = `safeCross`, 모퉁이 목적지 8곳 `arrive`. 차에 닿으면 즉시 종료.
* 보행 신호등: `signals.pedRemain(node, crossAxis)` 잔여 시간, 끝 3초 전 녹색 깜빡임, `TG.tex.pedHead(walk, n)` 서 있는/걷는 사람 픽토그램 + 잔여 초 숫자. HUD `section` 에 앞 횡단보도의 보행 신호·잔여 초.
* 조작 배치 `settings.ctl` stick|pad: `body.padctl` 이면 `#pad`(십자키 `data-btn` + △○×□·L1/R1·SELECT/START `data-key` → 키 핸들러 호출) 를 쓰고 스틱·버튼을 숨긴다. 설명 창 `#ctlHelp`(`G.showCtlHelp`, 게임 중엔 일시정지 사유 'help').
* 실물 게임패드: `input.pollGamepad()` 표준 배치(왼쪽 스틱 조향·RT/A 가속·LT/B 브레이크·X 단속·Y 앰프·L1/R1 깜빡이·십자=방향키·Start=Esc·Select=시점·L3 경광등·R3 주행 모드). 연결 시 `input.onGamepad` 로 안내. `readMove()` 가 보행자 이동 벡터(스틱·십자·패드 왼쪽 스틱·Shift/A 달리기).

## v0.7.3 추가(2026-09-07) — 12항목 소재 완성 · 건널목 · 침수 · 어린이 교실 · 도보 단속

* 교통 AI 습관 추가(`traffic.js` makeCar `trait`): `drunk`(차로 안 비틀거림 `car.weave` → extra, 목격 5초 → 'drunk'), `overtake`(느린 앞차를 우측 차로로 추월 → 'overtake', 랜덤 차로 변경은 이 습관 차량엔 없음), `sidewalk`(30초 주기 7초 보도 주행 → edgeRider 재사용, 'sidewalk'),
  트럭 `cargo`(짐칸 상자 3개, 15~25초마다 낙하 → litters 에 life 20초, 'cargo'), 버스 `door`(열린 문 + 문가 승객, 달리며 3초 목격 → 'passenger'). `noLicense`(8%)는 고지 완료 때 MDT 면허 조회로만 드러난다(+15).
  `spawn(opts)` 는 `trait`·`noLicense`·`type`·`color` 를 makeCar 로 넘긴다(전엔 버려졌다 — 체험 장면이 안 되던 원인).
* 철길건널목 `js/rail.js` `TG.Rail(scene, terrain, link, idx)`: 서초대로 연장(`conns[3]`) 샘플 22. 주기 58초(열림 38 → 경보 3 → 닫힘 17, 열차 통과). `approach(car, cur)`(AI: 정지선, 위반 성향 70% 통과 → 'railroad'), `distFor(x,z,h)`(플레이어: 통과 시 −10, 열려 있어도 일시정지 안내), `forceClose/forceOpen`(테스트·체험). 경보종 `TG.audio.bell`.
* 양재천(`terrain.yjZ`, `river()` 두 번째 항, 남부순환로 남쪽 z≈356) + 산책로. 비(`weather.set('rain')`) → `terrain.setFlood(true)`: 수면 +1.2m(양재천·한강), `isWater` 임계 상승(둔치·산책로 침수 → 빠지면 복귀), 잠수교 통제 표지·차단봉(`jamsuGroup`), 산책로 「통제」 표지.
  잠수교 낮은 상판은 강을 건너는 첫 연결로(`terrain.jamsu.link`, 축약 지도에선 한남대교) 아래 −0.55m.
* 모드 `kid`(🧒 어린이 보행 교실): `TG.Walker(..., {kid:true})` 작은 몸·노란 모자·책가방, 학교 블록 모퉁이 4곳(학교 정문→놀이터→문방구→우리 집). 감점 없음, 횡단마다 별(초록불 1 + 멈춤 0.8초 1 + ✋ 손 들기 1). `kidStep()` 단계 칩(`#kidSteps`), `kidVoice()` 쉬운 말 음성, `#btnHand`(G 키) 손 들기, `walker.raiseHand`.
* 도보 단속: walk 모드에서 위반 차량 터치/단속 버튼 → 퀴즈 → 정답이면 `enforcement` 가 수신호 정차(`onFoot()`: 경광등 조건 없음, 운전석 옆 3.6m 안에 서면 고지 완료). enforcement 는 `game.actor()` 로 걷는 경찰관을 본다.
* 타이틀 정리: 부제 「서울교통 순찰근무」, 모드 6개만 보이고 차량·시점·날씨·조작은 `<details class="more">` 안. 설정 v3: 차량 `random`(`carSpec()`), 날씨 `auto` 기본. laws.json 에 overtake·railroad·license·drunk·sidewalk·passenger·cargo(모두 verified:false), study12 7개에 scene 연결.

## v0.7.4 추가(2026-09-07 밤) — 캐릭터 리그 · 홍보 페이지

* `js/character.js` `TG.Character.build('officer'|'kid'|'civilian', opts)` → 관절 리그(목·어깨·팔꿈치·엉덩이·무릎 9관절, 얼굴·정모/모자·조끼·「POLICE」 라벨·장갑·신발·책가방). `animate(rig, {speed, moving, hand, gesture:'stop'|'go'|'wave', look, lookScan}, dt)` 가 걷기(무릎·팔꿈치·몸통 흔들림·숨쉬기)·손 들기·수신호·시선·두리번을 만든다.
  `TG.Character.lite(opts)` 는 행인용(메시 5개, 얼굴·치마·가방·모자) — `peds.js` 가 쓴다(limbs 4개 API 유지, 발 높이 0.02). GeoBuilder 에 `sphere()` 추가.
* `walker.js` 는 리그를 쓴다(`walker.rig`, `gesture`, `look`, `lookScan`). main `walkUpdate` 가 수신호(정차 유도 중 stop, 풀 때 go)와 시선(대상 차량)을 넣는다.
* 음성: `TG.audio.say(text, {kind:'officer'|'kid'|'pa'|'narrator', queue})` — 한국어 목소리 자동 선택(Google/Neural 우선), 화자별 높낮이. `pa()` 는 say 를 쓴다. main `LINES`/`pickLine` 으로 같은 뜻의 말을 돌려 쓴다(kidVoice 키: stop/hand/wait/go/red/green/road/good1~3, kidSay 'kidOk').
* 홍보 페이지 `promo.html` + `js/promo.js`: 게임 본체 없이(main.js·hud·input 미포함) 같은 도시에서 경찰관·어린이가 「횡단보도 5원칙」을 50초 루프로 시연(자막·음성·차량 정지·마지막 QR 카드). `?test=1` 로 헤드리스 검증. `js/qr.js` `TG.qr.encode/draw`(티북 QR 과 같은 인코더).
* 랜드마크 추가: `'0,1': 'terminal'`(고속버스터미널) · `'0,2': 'gu'`(서초구청) — world.js 에 그리기.

## v0.7.5 추가(2026-09-07 밤) — 게임 감각·효과음·표정·동행 경찰관

* 효과음 팩(`audio.js`, 전부 합성): `footstep(run, surface)`(왼발·오른발 음색 다름) · `tick(on)`(방향지시등 릴레이) · `crossSignal('cuckoo'|'cricket')`(횡단보도 음향신호기: 남북 뻐꾸기·동서 귀뚜라미) · `jingle(n)`(별·정답 아르페지오) · `pop()`(점수) · `whoosh()`(모드 전환) · `horn()` · `shutter()`(위반 포착) · `rain(on)`(빗소리 루프) · `bell()`. `TG.audio.speaking` 은 지금 말하는 화자(officer/kid/narrator/pa).
* 표정(`character.js`): 눈·입은 별도 메시. 3~6초마다 깜빡임, `talking` 이면 입이 열렸다 닫히고, `smile` 이면 입이 넓어진다. `TG.Character.actor(scene, terrain, kind, x, z, h)` 자율 배우(goTo/face/lookAtPos/gesture/smile, `update(dt, talking)`) — 홍보 장면과 어린이 교실 동행 경찰관이 같이 쓴다.
* main: `addScore` → `hud.pop`(떠오르는 점수)+`pop` 소리, 위반 목격 → `hud.flash`+셔터+`G.punch`(FOV 펀치), 충돌 → `G.shake`(`camFx(dt)` 가 두 카메라 끝에서 적용), 깜빡이 릴레이 소리, 비 오면 빗소리, 모드 버튼 휙 소리, 출동 버튼 맥동(CSS).
  걷기 모드: 발소리(`walker.rig.ph` 반 바퀴마다, `walk.onRoad` 면 노면 소리), 앞 횡단보도가 보행 녹색이면 음향신호기(`walk.greenAxis`), 별 획득 시 `hud.burst('⭐')`+`jingle`+웃음(`walk.smileT`).
  어린이 교실 `walk.officer`(동행 교통경찰관): 아이 왼쪽 뒤 1.3m 를 따라 걷고, 아이가 건널 땐 「정지」 수신호, 별을 따면 손 흔들며 웃는다. 안내 음성(narrator)이 나올 때 입이 움직인다. start() 에서 dispose.

## v0.7.6 추가(2026-09-08) — 보행 신호등 현실화 · 차량 감각 · 결과 카드 · 경적 · 홍보 장면 2

* 보행 신호등(`world.js`): 횡단보도마다 양쪽 연석에 하나씩(노드당 8개), 기둥 3m·함체·차양·작동 버튼함, 머리(잔여시간)는 길 건너편을 향한다(`rotation.y = atan2(-ps*r, ...)`). `world.heads` 의 ped 항목이 2배가 되었다(signals.update 가 그대로 돈다).
* `js/vfx.js` `TG.VFX(scene)`: 입자 풀(THREE.Points 160개, `puff(x,y,z,vx,vy,vz,sec,size)`) + `attachFlares(mesh,w,l,y)`(가산 스프라이트 2개). main: 내연기관 급가속 배기(속도 <9), 미끄러짐 타이어 연기(`telemetry.skid>0.35`), 밤이면 전조등 플레어(추적 시점만). 텍스처 `TG.tex.smoke/flare`.
* 결과 카드(`hud.showEnd`): `stats.stars`(1~5)·`stats.badges`(무사고·단속왕·정확한 판단·보행자 지킴이·신호·속도 준수·모범 보행 / 어린이: 횡단보도 박사·무사히 집까지·안전 보행) — `endShift` 가 계산. 4별 이상이면 팡파르.
* AI 경적(`traffic.js`): 앞차가 3~7초 서 있으면 성질 급한 운전자가 울린다(플레이어 45m 안에서만 소리), 보행자 급제동 때 짧은 경적.
* 타이틀: 카메라가 숨 쉬듯 오르내리고 제목 글자에 반짝임(CSS shimmer).
* 홍보 페이지 장면 2 「이륜차 보도 통행」(`MOTO_STEPS`, `?scene=moto`): 동쪽 보도로 북상하는 오토바이(edgeRider)를 경찰관이 수신호로 세운다. 장면 1(50초) → 장면 2(30초) 번갈아 자동 재생. 헤드리스 테스트는 82초를 돌려 두 장면을 거친다.

## v0.7.7(2026-09-08) — 보행 조작·보행 신호 시간·걸음걸이

* 신호 주기: `SIG_GREEN 24 · SIG_YELLOW 3 · SIG_ALLRED 1.5`, `PED_WALK 20`(왕복 4차로 20m 를 어린이 걸음 1.25m/s 로 건널 시간). 주기 57초.
* 보행 조작(소유자: 「살짝 밀면 살짝 휘어야, 시야가 확 돌면 MMORPG 유저가 욕한다」): `walker.update` 회전은 최대 ≈3.2rad/s·작은 각은 느리게, 3인칭 카메라 방향 `walk.cyaw` 는 헤딩을 1.3rad/s(급회전 3.0, 정지 0.5)로 따라간다. 이동 기준은 실제 카메라 방향 `walk.camYaw`(walkCamera 반환값) 그대로.
* 달리기는 토글 버튼 `#btnRun`(🚶/🏃, `input.runToggle`)·Shift·패드 A 로만. 스틱 끝까지 밀어도 걷는다. 어린이 교실: 횡단보도에서 뛰면 「뛰지 말고 걸어요」 + 걷기 ✗(별 −1, 최소 1).
* 걸음걸이: 팔은 반대쪽 다리와 함께(`character.js` lx = sin(ph)·rx = −sin(ph)). 이전엔 같은 쪽 팔·다리가 같이 나갔다.

## v0.7.8(2026-09-08) — 서초구 지명 · 경부고속도로 · 차량 팝인 방지

* 도로명(`city.js`): 남북 반포대로·서초중앙로·강남대로·논현로·언주로 / 동서 신반포로·사평대로·서초대로|테헤란로·효령로·남부순환로. 교차로 이름 `NODE_NAMES`(고속터미널·서울성모병원·서초역·교대역·강남역·역삼역·반포·서초3동·남부터미널 사거리). 랜드마크 terminal 은 '0,0'(신반포로·사평대로 사이). 미니맵에 역 이름.
* 경부고속도로(`terrain.js` ICS S)는 `kind: 'highway'`(편도 3차로 + 1차로 버스전용, 100km/h). 버스는 1차로로 22m/s 로 달린다. 연결로 이름: NE 언주로·청담대교 방향, SW 반포대로 연장·양재 방향.
* 차량 팝인/팝아웃(`traffic.js`): 링크 스폰은 플레이어 앞 시야 140m 안에서 하지 않고, 시야 앞의 차는 despawn 거리의 1.8배까지 남긴다(눈앞에서 사라졌다 나타나던 현상).

## v0.7.9(2026-09-09) — 경찰 대응 원칙(추격은 최후의 수단) · PM · 탑승자 사람 리그

* `js/response.js` `TG.Response(game)` — 등급: **A**(음주 의심·무면허·수배차량) 무전 전파 후 정차 유도·추격 허용 / **B**(일반 차량 위반) 정차 유도 단속 / **C**(이륜차·자전거·PM 단순 위반) **추격 금지 → 📹 영상 + 📡 무전**.
  `blackbox(car)`(B 12점·C 25점, 위반 표시 해제) · `radio()`(8점, 등급 C 는 6~10초 뒤 인접 순찰차 인계 +10, 등급 A 는 `car.pursuitOk` = 추격 정당화, 수배차량은 이때 드러난다) · `update(dt)`(등급 C 를 사이렌·38km/h·30m 안에서 5.5초 쫓으면 `pursuitBan` −15, 등급 A 를 무전 없이 8초 쫓으면 `noRadio` −8, 보행자 5m 근접 경고) · `adviceFor(car)`(MDT 노란 줄).
  버튼 `#btnCam`(B 키) · `#btnRadio`(T 키). enforcement 퀴즈 정답 뒤 등급 C 는 정차 유도 대신 영상 단속으로 간다. 결과 카드에 영상·무전·인계 줄과 배지(📹 원칙대로 대응 · 📡 상황 전파).
* 개인형 이동장치(PM, 전동킥보드): `vehmesh TYPES.pm`(발판·T핸들), `traffic` isPM(보도 통행 `pm`, 헬멧 미착용 `pmHelmet`, 2인 탑승 `pmTwo` — 보도 통행이 기록돼 있으면 덮지 않는다), laws.json 3항목(모두 확인 중), 퀴즈 보기, 앰프 문구.
* 이륜차·자전거·PM 탑승자는 사람 리그로 태운다: `vehmesh twoWheeler(..., {noRider:true})` + `TG.Character.build('civilian', {helmet})` + `TG.Character.pose(rig, 'ride'|'stand')`. 몸통 메시 원점이 발끝이라 `parts.torso.rotation.x` 를 크게 주면 상체가 꺾인다 — 자세는 관절(엉덩이·무릎·어깨·목)로만 만든다.
* 수배차량 `car.wanted`(승용 2%): 겉보기 표시 없음, 📡 무전 조회로 드러나고 그때부터 추격이 정당해진다.

## v0.8.1(2026-09-09) — 도로 차로 수(서초구 실측) · 8차로 고속도로 · 연결부 · 미니맵 · 고장차량·사고 · 경찰 표장

* 도로마다 편도 차로 수(`config.LANES_V/H`, `LANE_W 3.5`): 반포대로·강남대로·남부순환로 왕복 8차로, 서초대로(테헤란로) 왕복 6차로, 나머지 왕복 4차로.
  `city.halfOf = 3 + 3.5n`, `laneOff(axis,idx,k) = 2.0 + 3.5k`, `shoulderOff = 3.5n + 1.65`, `laneIndexAt(axis,idx,lateral)`. `AVENUE_V/H`·`ROAD_HALF4` 는 더 쓰지 않는다.
  world.js 차로 점선은 n−1 개, 가장자리 실선은 `0.25+3.5n`, 신호등 암 길이는 차로 수에 맞춘다. traffic 차로 변경은 옆 차로 한 칸씩, 우회전은 가장 바깥 차로에서만. main 8-2 는 `city.laneIndexAt` 로 판정.
* 고속도로 왕복 8차로(`HW_LANES` 4개, `HW_HALF 17`, `HW_SHOULDER 15.65`). **버스전용차로는 경부고속도로만**(`link.busLane`) — 올림픽대로·순환고속도로에는 없다. 차로 배정·위반 판정·청색 실선 모두 이 플래그를 본다. 버스는 전용차로에서 정차하지 않는다(kind 'highway' 순항).
* 연결부 꺾임 수정 2건: IC 연결로 시작점을 `city.EXT`(가장 넓은 도로 기준 스텁 길이)로 통일해 도시 스텁 끝과 정확히 맞물리게 하고, 도시에서 곧게 나가는 제어점 길이를 첫 경유점 거리의 45%로 제한(고정 45m 면 경유점을 지나쳐 스플라인이 되돌아 꺾였다 — 차가 튕겨 나가던 원인). IC 자동 주행 32/32·AI 8/8.
* 미니맵 현재 위치: 맥동 고리 + 어두운 원 + 흰 테두리 화살표 + 왼쪽 아래 「▲ 현재 위치」 범례(확대 변환 밖). 도로 두께는 차로 수에 비례.
* 고장차량·교통사고 현장: `traffic.spawnIncident('broken'|'crash', at)` — 갓길에 비상등만 켠 차(`mode 'incident'`) + 안전삼각대 + 라바콘 2개, 상황실 신고(`onEvent('incident')`). 처리 절차는 `response.incidentUpdate`: 경광등 ON + 현장 뒤 3~22m 정차 + 📡 무전(견인·구급 요청) → 2초 유지 → 안전조치 완료(+30), 6초 뒤 현장 정리. 순찰 근무에서 70~110초마다 하나. 결과 카드 줄·배지(🛠 현장 안전조치).
* 경찰 표장(`textures.drawEmblem`): 금색 이중 테두리·점선 고리 + 아치 글씨(「경 찰 청」·KOREAN NATIONAL POLICE AGENCY) + 참수리(각진 깃 5장 날개·갈고리 부리) + 저울 막대·접시 + 무궁화 5장 + 태극. 캔버스 512로 키웠다. 외부 이미지 파일은 여전히 0개(에셋 0 규칙) — 첨부 사진은 참고용으로만 썼다.

## v0.8.2(2026-09-09) — 교차로 근무(하차 근무): 신호기 수동 조작 1단계 · 차로 차단·꼬리 끊기 2단계

* 모드 `duty`(🚦 교차로 근무). `startDuty()`: 서울성모병원 사거리(`city.nodes[0][1]`)에 순찰차를 갓길에 **경광등 켠 채** 세우고, `TG.Walker` 로 하차해 근무한다(`onFoot()` 에 duty 포함 — 보행 카메라·조작·발소리를 그대로 쓴다). 제한 시간 `DUTY_SECONDS 420`, `TRAFFIC_MAX + 26`.
* `js/junction.js` `TG.Junction(game)` — 대기 행렬 `queueOf(node,d)`(정지선 뒤 80m, 2m/s 미만, 내 차로만), 꼬리물기 `gridlockOf(node)`(교차로 상자 안에 선 차), `stats(node)`, `dirsAvail(node)`(차가 들어오는 쪽이 있는 접근로만 — 지도 끝 노드는 3곳),
  `burst(node,d,count)` = **흐름 주입**(상류 교차로 쪽에서 1.1초마다 한 대. 스폰 최소 간격이 12m 이므로 한꺼번에 놓으면 만들어지지 않는다 — 대열은 정지선에서 다져진다), `closeLane(node,d)`(바깥 차로에 라바콘 7개 + `traffic.control.closed`), `openLanes()`, `setHand(node,d,on)`(`traffic.control.hand`), `placeBox(node,sx,sz)`(보도 위 모퉁이에서 9m — 횡단보도·보행 신호등 기둥과 겹치지 않는 자리), `nearBox`, `setBoxLamp`, `dispose()`(수동을 자동으로 되돌린다).
  채점: 10초마다 꼬리물기 0 + 최대 대기 8대 이하 → `junctionGood +15`, 꼬리물기가 이어지면 `junctionJam −6` + 6초 경고.
* `js/signals.js` 수동 조작: 교차로 고유 최소 녹색 `NODE_MIN`(성모병원 18 · 서초역 16 · 교대역 15 · 강남역 20 · 역삼역 15 · 고속터미널 16, 기본 12), **보행 최소는 어디서도 줄일 수 없다**(`Math.max(minGreen, PED_WALK + 2)`).
  `setManual/isManual/request(node,axis)/waitFor(node,axis)/manualInfo/minGreenOf`. `update` 는 수동일 때 녹색 끝에서 시계를 멈춰 **녹색을 연장**하고, 요청이 최소 시간을 채우면 황색·전적색을 거쳐 넘어간다 — 버튼을 눌러도 즉시 바뀌지 않는다.
* `js/traffic.js` `this.control = { closed: [], hand: [] }`: 수신호가 있으면 **녹색이어도 정지선 앞에 선다**(제5조), 차단한 바깥 차로는 40m 앞에서 안쪽 차로로 옮긴다(라바콘 회피). 검증: 차단 뒤 정지선 부근 바깥 차로 0대.
* UI `#dutyPanel`(제어함 3.2m 안에서만 열림, 멀어지면 닫힘): 자동/수동 스위치 · 방향별 대기 대수·꼬리물기 칸 · 「↕ 남북 녹색 N초」「↔ 동서 녹색 N초」 요청 버튼(대기 이유: 보행 신호 최소 시간 / 최소 녹색 시간 / 황색·전적색 통과) · 🚧 바깥 차로 차단 · ✋ 꼬리 끊기. 버튼 `#btnBox`(R) · ✋ 는 duty 에서 꼬리 끊기(G).
* 결과 카드 배지 `🚦 소통 확보 N회`(3회 이상, 5회 금색). `_verify_tail.js` 5o 가 제어함 거리·자동에서의 거절·최소 녹색 26초·2초 뒤 미전환·30초 뒤 전환·녹색 연장·대열 8대·차로 차단(라바콘 7)·수신호·정리까지 확인한다.

## v0.8.3(2026-09-09) — 웅장한 인트로 사운드 · 타이틀 테마

* 소유자 「인트로 화면에서 웅장한 사운드도 필요한데」 → `audio.js` 테마 확장(파일 0개 유지).
  ① `reverb()` — 컨볼루션 대신 딜레이 4개를 되먹인 콤 필터 홀(폰에서도 가볍다). 테마는 센드로 보낸다. ② 테마 버스에 `DynamicsCompressor`(합주가 뭉쳐 크게 들린다).
  ③ 금관 모티프 D–A–D–F♯: 3초에 낮게 예고, 10.55초 타이틀 순간에 옥타브 위 + 5도 화음, 12.7초 지속음(`brass(at,dur,freq,vol,pan)` = 톱니 2겹 + 사각파 + 필터 엔벨로프).
  ④ 합창 패드(사인 스택 8성 + 4.6Hz 떨림) 8.4초 진입. ⑤ 타이코 2연타·마지막 대타격 2겹(140Hz + 62Hz). ⑥ 좌우 폭(`createStereoPanner`, 없으면 게인). 길이 14.5초(인트로 15초).
* `titleTheme()`/`stopTitleTheme()` — 타이틀 화면용 16초 루프(드론 D + 0.75초 아르페지오 D–F♯–A–D + 6초마다 낮은 북, 음량 0.5). 타이틀이 무음이면 게임이 꺼진 것처럼 보인다.
  main 루프의 title 분기에서 소리가 풀리는 순간 시작하고, 출동·근무 종료에 끈다. 소리가 아직 잠겨 있으면 타이틀에도 「🔊 화면을 한 번 터치하면 소리가 납니다」를 띄운다(전에는 인트로에만 나와서 소유자가 테마를 못 들었을 수 있다).
* 헤드리스에서 소리 코드까지 실제로 돌려 확인하려면 `--autoplay-policy=no-user-gesture-required --mute-audio` 를 붙인다(그냥 돌리면 `audio running = false` 로 테마 그래프가 만들어지지 않는다).

## v0.8.4(2026-09-09) — 추격전 모드(모드 구조 = 근무 / 교차로 근무 / 추격전 / 서킷 / 자유 / 보행·어린이)

* 소유자 질문 「근무 모드 서킷 모드 추격전 뭐 이렇게」에 대한 답. 추격전은 **대응 원칙(v0.7.9)의 실습판**이다 — 빨리 달리는 재미는 있지만 이기는 방법은 난폭 운전이 아니다.
* `js/chase.js` `TG.Chase(game)`: 대상은 등급 A(수배·절도·음주 의심)뿐. `spawn()` 이 플레이어 앞 55m+ 도로에 `wanted/flee/chase` 차량을 만든다(`traffic.js` 에서 `car.flee` 는 흐름의 1.6배, 앞차·보행자 앞에서는 그대로 선다. `car.chase` 는 멀어도 despawn 하지 않는다).
* 채점(`config.SCORE`): `chaseSafe +15`(경광등 ON + 📡 전파 + 대상이 앞 + 10~42m 를 10초마다), `chaseClose −10`(8m 안 1.2초마다), `chaseReckless −25`(추격 중 차량 접촉), `chaseBreak +45`(추격 중단이 정답인 경우), `chaseCatch +60`(원칙 유지 20초 → 대상 포기·정차).
  중단이 정답인 경우: **어린이보호구역으로 도주**(자동 판정) · 차량 접촉 2회 뒤 상황실 지시(경광등 끄고 45km/h 미만으로 감속하면 중단 처리). 놓치면 무전 전파로 인계('lost').
* 감점 게이트 수정: `penalize` 는 이제 `SCORED = {patrol, walk, duty, chase}` 만 감점한다(전엔 `mode !== 'patrol' && mode !== 'walk'` 라 교차로 근무·추격전의 감점이 통째로 무시됐다). 타이머도 patrol·chase 가 카운트다운하고, `setTimerText('∞ 자유 주행')` 는 free·circuit 에만 쓴다.
* 결과 카드 배지: `🛑 중단 판단`(금) · `🚨 안전한 추격`(무사고 검거, 금) · `📏 안전거리 N회`. 교훈 줄도 결과별로 다르다.
* `_verify_tail.js` 5p: 대상 생성·8m 근접 감점·**무전 없이는 원칙 유지 시간이 쌓이지 않음**·전파 후 20초 → 검거·부수적 피해 2회 → 중단 지시·어린이보호구역 도주 → 중단(+45)·정리까지.

## v0.8.5(2026-09-09) — 자전거·PM 은 횡단보도에서 내려서 끌어야 보행자 · 어린이 교실 손 들기는 선택

* 소유자 지시: 「횡단보도를 자전거나 PM 이 보행자가 되려면 내려서 밀면서 걸어가면 보행자가 되지만 타고 가면 차량이 되기 때문에 내려서 밀면서 걸어야 함. 손은 꼭 들 필요가 없지만」.
* 근거는 **티북 v21.94 「자전거 타고 횡단보도」 카드의 조문 인용**을 그대로 썼다 — 도로교통법 **제13조의2 제6항** 「자전거등의 운전자가 횡단보도를 이용하여 도로를 횡단할 때에는 자전거등에서 내려서 자전거등을 끌거나 들고 보행하여야 한다」(2020.6.9 개정 · 조문시행일 2026.7.1 현행).
  탄 채로 건너면 그 사람은 「자전거등의 운전자」여서 보행자가 아니고, 치여도 교특법 §3②6호 12대 중과실(횡단보도 보행자 보호)은 성립하지 않는다. 사고 조사에서는 충돌 직전 탑승 여부를 블랙박스로 특정한다.
  → `data/laws.json` 에 `bikeCross` 추가(조문은 `verified: true`, 범칙금·벌점은 별표 미대조라 `verified: false`), 퀴즈 보기(자전거·PM), 대응 등급 **C**(추격 금지 → 📹 영상 · 📡 무전).
* `traffic.js`: 자전거·PM 의 절반은 `car.crossRider`(타고 건넘 = 위반 기록), 나머지는 **내려서 끌고 걷는다** — `setPush(car,on)` 이 탑승자를 기계 왼쪽에 세우고(`pose 'stand'`) 걷는 자세로 애니메이션, 속도 1.35m/s, 보행 신호가 적색이면 횡단보도 띠 앞에서 기다린다.
  판정은 **가까운 노드와 진행 방향으로 직접** 잰다 — 경로점(`ap`)은 정지선을 지나면 다음 교차로로 넘어가 버려서 횡단보도 구간을 놓친다(이 함정으로 첫 구현이 한 번도 발동하지 않았다).
* `car.edgeOff`(보도 주행 옆 이동량)를 도로 폭에 맞춰 계산한다: `city.sideOff − city.laneOff(laneIdx)`. 고정 5.4m 이면 8차로 도로에서는 바깥 차로에 있을 뿐 보도가 아니었다(8차로 기준 16.5m).
* 어린이 교실: 단계가 🛑 멈춰요 → **👀 좌우를 살펴요**(멈춰 선 채 1.6초) → 🔴 기다려요 → 🟢 건너요 로 바뀌었다. **손 들기는 더 이상 필수가 아니다**(별 = 멈춤 + 초록불 + 걷기). 손을 들면 「꼭 들 필요는 없지만 운전자가 나를 더 잘 봐요」 + 5점, 2회 이상이면 배지 ✋.

## v0.8.6(2026-09-09) — 법령 데이터를 티북에서 가져왔다(추측 금지)

* 소유자 지시: 「티북앱에 있는 내용들을 게임에 구현을 해야 하는데 좀 참고 하면서 만드는게 어떨지 싶다. 그냥 막하지 말고」.
* `C:\Users\knpth\Desktop\지식베이스\20260909_v21.94_배포용\index.html`(티북 최신 판)에서 게임의 위반 25종 + study12 12종의 **조문·범칙금·벌점·판례 사건번호·현장 요령**을 찾아 `data/laws.json` 에 옮겼다. 지어낸 값은 없다.
  채운 방식: `law.article` + `law.source = "T-Book v21.94(원문 대조 2026.9.2 …)"`, `fine.<차종>` + `fine.source = "시행령 별표8 제N호 …"`, `points.value` + `points.source = "시행규칙 별표28 제N호"`, 새 필드 `precedent`(사건번호와 요지) · `field`(티북의 「현장」 요령) · `penalty`(무면허·음주처럼 범칙금이 아니라 형사처벌인 항목).
* **티북이 스스로 미확인이라고 적어 둔 것은 그대로 미확인으로 남겼다**: unsafe(제48조 단독 금액 없음) · jaywalk(「범칙금 — 현행값 법령DB 확인」) · motorcycle(이륜차 표 금액 공란) · pm(보도 통행 금액 없음) · bikeCross(금액 없음) · pmTwo(「범칙금액은 별표8 미대조」 → `candidate` 4만원). license·drunk 는 `penalty` 로 형사처벌을 보여 준다.
* 판례가 없는 항목은 티북 문구 그대로 「대조된 대법원 판례 없음(2026.9.2 기준) — 조문으로 의율」이라고 적었다. 화면에는 `precedent` 가 「대법원」으로 시작할 때만 「판례 —」 줄을 보여 준다(없음 문구를 띄우지 않는다).
* `enforcement.lawLines` 가 조문 → (처벌 또는 범칙금·벌점) → 교훈 → 「현장 —」 → 「판례 —」 순으로 보여 준다. 보행자 범칙금은 「30,000원」으로만(차종 라벨 중복 제거).
* 다음에 법령을 다룰 때도 **먼저 티북 최신 판을 grep** 한다. 기억으로 숫자를 채우지 않는다. 티북의 총괄 단서: 범칙금은 승용 기준(승합 +1만·이륜 −1~2만·자전거등 별도), 벌점은 별표28, 최종 집행 시 국가법령정보센터 재확인.

## v0.8.7(2026-09-09) — 어린이 4단계와 자전거 규칙을 섞지 않는다(소유자 정정)

* 소유자 정정: 「자전거는 멈추고 손들고가 아니라 횡단보도에서 내려서 자전거를 밀면서 가야 보행자가 되는거야. **어린이만** 멈춘다 본다 손을든다 걷는다가 되는거지」.
  → v0.8.5 에서 「손 들기는 선택」으로 바꾼 것은 자전거 이야기를 어린이 교실에 잘못 적용한 것이었다. **되돌렸다.**
* 어린이 교실 단계는 소유자의 말 그대로 4단계다: 🛑 **멈춘다**(0.8초) → 👀 **본다**(1.6초, 좌우 살피기) → ✋ **손을 든다**(필수 단계) → 🚶 **걷는다**(초록불, 뛰지 않음).
  적색이면 손을 든 채 「🔴 손 들고 초록불을 기다린다」로 대기한다. 별 3개 = 멈춤 + 손 들기 + 걷기(초록불은 전제). 칩·음성·결과 교훈 문구도 이 네 단어로 맞췄다.
* 자전거·PM 은 이 4단계가 아니다 — **내려서 끌거나 들고 걸어야 보행자**(제13조의2 제6항, v0.8.5 의 `traffic.js` 규칙). 두 규칙을 한 화면에서 섞어 말하지 않는다.
* `promo.html` 의 「횡단보도 다섯 가지 약속」(멈춰요 → 살펴요 → 손을 들어요 → 초록불을 기다려요 → 초록불에 건너요)은 이미 같은 순서라 그대로 두었다.

## v0.9.0~v0.9.1(2026-09-09) — 인트로 연출 · 실물 경찰 마크·복장·순찰차 뒷모습 · 신호제어기 실물화 · 말의 출처 구분

* **인트로(`js/intro.js` `TG.Intro`)** — 소유자 「인트로를 멋있게, 상용게임 느낌」. 샷 표(SHOTS)로만 카메라를 움직인다: ① 도시 항공 강하(조문 콜드 오픈) → ② 경광등 켠 순찰차 트래킹 → ③ 하차 근무(제어함 앞 경찰관) → ④ 어린이 횡단(손 든 아이 + 정지 수신호) → **10.4초 테마 대타격에 맞춰 타이틀 등장** → ⑥ 크레인 아웃 + 「근무 연습 · 스트레스 해소 · 교통안전 홍보」 → ⑦ 정착. 17.2초.
  레터박스 바(`body.cine-out` 으로 열림)·하단 3분할 자막(`#introShot`)·타이틀 등장(`#introTitle`, 표장 스탬프 + 로고 자간 확장 + 흰 플래시)·비네트. 인트로는 **석양 고정**(`weather.set('sunset')`, 인트로 루프에서 `weather.update` 를 직접 돌려야 색이 적용된다).
  배우·제어함은 실제 게임 오브젝트를 쓴다(`TG.Junction.placeBox`, `TG.Character.actor`). 카메라는 **도로 위(|x|<17)에서만** 움직인다 — 블록은 전부 건물이라 그 위를 지나면 화면이 막힌다. 타이틀 컷은 도로에서 곧게 솟구쳐 건물을 피한다.
  함정 2건: `player.pos.y` 는 없다(`player.y`) — NaN 이 되어 화면이 검게 나왔다. `world.js` 신호등 머리는 `material: null` 로 만들어져 signals.update 전에 렌더하면 three 가 죽는다 → 보이지 않는 임시 재질(`headHold`)을 물렸다. 스틸 검증용 `T.freeze(on)` 훅 추가.
* **경찰 마크(`textures.js`)** — 소유자가 실물 이미지를 제공하고 「저작권이 없으니 정확하게 표현」하라고 지시. 두 형태로 다시 그렸다: `drawShield`(방패형 「경찰 POLICE」 — 순찰차 도어·HUD·타이틀, `emblem()`) · `drawEagleMark`(참수리 + 무궁화 + 태극 — 신호제어기·홍보, `emblemEagle()`). 공통 부품 `eagle(span)`·`mugunghwa(r)`·`taegeuk(r)`. 여전히 캔버스로만 그린다(에셋 0).
* **교통경찰 복장(`character.js`)** — 참고 사진대로: 정모 흰 덮개 + **금색 밴드** + 검정 챙 + 참수리 표장, 형광 연두 조끼 + 은색 반사 띠, 등판 「교통경찰 / POLICE」 청색 판(`tex.vestLabel`), 흰 장갑, **적색 신호봉**(수신호할 때만 든다). 얼굴에 볼 홍조. 어린이는 치비 비율(머리 크게·팔다리 짧게·눈 크게, 전체 0.62배).
* **순찰차 뒷모습(소유자: 「뒷모습 매우 중요해」)** — `tex.liveryRear()` 후부 반사판(형광 연두·적색 사선 + 청색 POLICE·112)을 뒷면에 붙였다. **승강식 전광판**: 지붕에서 가위형 팔이 올라오고 판 뒤쪽에 호박색 점자식 글씨 + 화살표(`tex.ledBoard`). `player.setSign(on, text, arrow)`·`signUpdate(dt)`, V 키. 사고·고장 현장 30m 안에서 자동으로 올라가고 안전조치가 끝나면 내려간다.
* **신호제어기(소유자 제공 실물 사진)** — 회색 함체 + 남색 상단 캡 + 참수리 표장 + 「경찰청 표준 교통신호제어기」 명판(`tex.ctrlPlate`) + 원형 시건장치 + **가운데 조작문이 위로 젖혀 열리고** 안에 자동/수동 토글 4개와 붉은 누름버튼 2개. 조작판을 열면 실제 문이 열리고, 수동으로 바꾸면 첫 스위치가 내려가고 버튼에 불이 들어온다(`setPanel`·`boxAnim`).
* **말의 출처를 구분한다(소유자 지시)** — 💭 속말(혼잣말·판단, `officerSay` 가 힌트에 표시) · 📢 앰프(`paSay`, 차임 + 자막 「📢 앰프 —」) · 📡 무전(`TG.audio.squelch()` 스퀄치 + 자막 「📡 무전 —」). 확성기 호칭은 `addressOf(sel)` 한 곳에서만 만든다(앞 차량·앞 버스·앞 화물차·이륜차·자전거·킥보드·보행자) — **없는 말을 만들지 않는다**. 어린이 칭찬에서 「완벽해요!」를 뺐다.
* 홍보 페이지 장면 2 에 「자전거·킥보드는 내려서 끕니다」 컷을 넣고, 이륜차 단속 장면에서 어린이 칭찬 말투를 뺐다.

## v0.9.2(2026-09-09) — 순환고속도로 연속성 · 고속도로 진입부(가속차로) · 하차·승차 · 추격전 재미 층 · 고화질 옵션

* 소유자 보고 「고속도로 연결부분 도로가 중간중간 끊겨 있고, 순환고속도로에서 달리다가 자동차가 사라진다. 계속 돌아도 문제가 없어야 하고 차로도 일관성 있게」 → 원인 셋을 찾아 고쳤다.
  ① **노면이 흙에 묻혔다**: `heightAt` 이 `nearest(x,z,true)` 로 가장 **가까운** 노면에 지형을 맞추다 보니, 고가 연결로가 순환고속도로 위를 지나가는 곳에서 지형이 고가 높이까지 올라와 본선을 덮었다(링 676샘플 중 17곳). → `nearest(x, z, allowRamp, lowest)` 모드 추가: 노면 폭 안에 드는 후보 중 **가장 낮은 노면**을 기준으로 지형을 깎는다. 검증: 링 묻힘 17 → **0**, 연결로 0.
  ② **차가 사라진다**: 링크(고속도로)에서 despawn 이 `DESPAWN×1.7`(289m) 이라 앞의 차가 눈에 보이는 거리에서 사라졌다 → `×3.0`(510m), 링크 스폰 상한도 `SPAWN_MAX×1.6` → `×3.2`(416m). 앞이 텅 비지 않는다.
  ③ **차로 표기 불일치**: 프레임 이름이 「순환고속도로(왕복 6차로)」로 하드코딩돼 있었다 → `cfg.HW_LANES.length * 2` 로 계산해 **왕복 8차로**로 맞췄다.
* **고속도로 진입부(가속차로)**: 램프가 j+4 에서 곧바로 본선 차로선에 붙어(15m) 진입이 꺾여 보였다 → 74m 뒤에서 시작해 26m 지점에서 완만히 휘고, **본선과 나란히 j+0~j+21(약 70m)** 달린 뒤 붙는다. 합류 구간은 안쪽 선이 점선이 되고 코 부분에 도류선을 넣었다. 램프 시작점은 연결로 끝(42m)과 정확히 맞물린다.
  주의: 가속차로를 본선 바깥으로 3.4m 내밀면 램프 노면이 본선 포장(17m) 밖으로 나가 `frameAt` 이 offroad 로 잡는다 — 나란한 구간은 본선 바깥 차로선(LO) 위에 둔다.
* **하차·승차(소유자 「내려서 근무하는 것」)**: 순찰·자유 주행·추격전에서 🚶 버튼(X)으로 내려 걷고, 운전석 옆 3m 안에서 🚓 버튼으로 다시 탄다. `G.afoot` 가 켜지면 `onFoot()` 이 참이 되어 보행 카메라·조작을 그대로 쓴다. `afootRules` 는 감점 없이 안전만 가르친다(차도에 서 있으면 「경광등을 켜고 갓길·보도로」, 차량 접촉은 즉시 종료).
* **추격전 재미 층(소유자 「니드포 스피드 느낌 … 재미를 줄 때는 확실하게」)**: `TG.audio.chaseTheme()` 추격 음악(132BPM 저음 펄스 + 16분 하이햇 + 두 화음 오스티나토, `chaseTension(0~1)` 으로 거리·속도에 따라 밝기·음량이 변한다) · 붉은 비네트(`body.chasing`) · **속도 화각**(추격 중 최대 +9°) · 검거 순간 **슬로모션**(0.35배 1.1초) + 화각 펀치 + 팡파르 · **아슬아슬**(다른 차를 2.6m 안으로 스치면 경고 팝 + 흔들림, **점수는 주지 않는다** — 난폭 운전을 칭찬하지 않는다).
* **고화질 옵션**: 일시정지 메뉴 「고화질」 → 픽셀 비율(터치 2 / PC 3)·그림자 맵 4096. 표준은 1.5/2·2048. `applyQuality()` 한 곳에서 정한다.

## v0.9.3(2026-09-09) — 고속도로 진입부를 원호로 · 타이틀에서 홍보 영상·인트로 다시 보기

* **진입·진출 램프의 실제 원인**: 램프가 연결로(방사 방향)에서 링(접선 방향)으로 90° 도는 구간을 제어점 3개로 30m 안에 밀어 넣어 **곡률 0.13(반경 7.7m · 권장 25km/h)** 의 하이핀이었다. 「고속도로 진입하는 모습이 영 이상하다」의 원인.
  → `ramps(conn, j, tag, split)` 를 다시 썼다: 분기점을 링에서 **56m** 떨어뜨리고(`SPLIT`), 반지름 `Rf = SP − LO`(약 43m)의 **원호 5점**으로 돌린 뒤, 원호 끝을 링 바깥 차로선의 실제 점(`ringLane`)으로 맞춰 붙이고 링과 나란히 22샘플(약 77m)을 달린다(가속차로). 램프 폭도 4.6 → **5.8m**.
  결과: 반경 22~42m(권장 45~65km/h)가 12곳, 대각 IC(NW·SE·NE·SW) 4곳은 아직 8~10m. IC 자동 주행 **32/32**·AI 8/8·오프로드 최대 2.6초.
  시도했다가 되돌린 것: ⓐ 가속차로를 본선 바깥 3.4m 로 내밀기(램프 노면이 포장 밖으로 나가 offroad) ⓑ 회전각을 실제 각도로 일반화(대각 IC 에서 오히려 나빠짐) ⓒ 정규 필렛(교점·접선 계산이 링 곡률 때문에 어긋남) ⓓ 후보 2개 중 곡률 작은 쪽 선택(대각 IC 는 둘 다 나쁨). **대각 IC 4곳은 다음 판 과제.**
* 타이틀 아래에 「📣 교통안전 홍보 영상」(promo.html 새 탭) · 「▶ 인트로 다시 보기」 링크를 넣었다 — 홍보용으로 인트로만 보여 줄 수 있다.

## v0.9.5(2026-09-09) — 추격 화면이 안 보인 버그 · 무전은 필수가 아니다 · 도주 차량의 먼지 · 화면 전수 점검

* **추격전 화면이 안 보였다(소유자 지적)**: `chaseUpdate` 가 `checkRules` **앞에서** 돌아 구간 이름(「강남대로(왕복 8차로) · 제한 50」)이 추격 줄을 매 프레임 덮어썼다. 순서를 뒤로 옮기고, 상단 대상 표시에도 「🚨 <대상> 추적 — Nm · 급제동(먼지)」 를 넣었다.
* **무전은 강제가 아니다**(소유자: 「추격 중 무전을 할 수도 못 할 수도 안 할 수도 있음. 단 공조를 통해서 검거할 때는 무전을 함. 모든 것을 무전보고하지는 않음」):
  - 안전거리 유지 시간에서 무전 조건을 뺐다. 무전 없이도 **단독 20초**로 검거된다.
  - 무전을 하면 **공조 12초**로 끝나고 +20점(앞을 막아 준다). 검거 문구도 「📡 공조 검거」/「단독 검거」로 갈린다.
  - `response.js` 의 `noRadio` 감점을 없앴다. 대신 4초 뒤 한 번 「혼자 쫓기 어려우면 공조를 부른다」로 권한다. `config.SCORE.noRadio` 는 남겨 두었지만 쓰지 않는다.
* **도주 차량의 먼지**(소유자: 「골목길에서 도주중인 차량은 급가속 급감속을 하기 때문에 먼지를 일으킴 — 먼지만 따라가도 잡을 수 있음」): `car.flee` 는 2.6초(골목)~3.8초 주기로 급가속(×1.75)과 급제동(×0.45)을 번갈아 하고, 그때 뒷바퀴 두 곳에서 먼지가 인다(급제동이면 더 크고 3.4초 남는다). 입자 풀 160 → 260. 멀리서 먼지 흔적만 보고도 따라갈 수 있다.
* **음주 도주차량**은 등급 A라 적당한 범위에서 추격이 가능하다 — 대상 이름을 「음주 의심 도주차량」으로 명확히 했다. 대상은 어린이보호구역 밖에서 시작하고, 보호구역 도주 중단 판정은 **5초 이상 따라간 뒤**부터(시작하자마자 중단이 뜨면 배우지 못한다).
* **화면 전수 점검**을 상시 검증에 넣었다(`_verify_tail.js` 5s): 순찰·하차 근무·승차 복귀·추격전·교차로 근무(조작판까지)·보행자·어린이(4단계 칩)·학습 카드·결과 카드·일시정지에서 보여야 할 요소가 실제로 보이는지 `getComputedStyle` 로 확인한다. 전부 ok.

## v0.9.6(2026-09-09) — 실제로 돌려 보며 잡은 완성도 결함들(소유자: 「실제로 해보고 상용게임 느낌과 완성도가 나와야」)

* **얼굴이 없었다(가장 큰 결함)**: `face()` 의 머리카락 구가 `r·1.02`(머리 구 `r` 보다 크다) + `sy 0.72` 라서 **적도에서 얼굴 앞으로 튀어나와** 눈·입·볼을 덮고 있었다. 게다가 눈·입이 `HR·0.9`(구 안쪽)에 박혀 있었다.
  → 뒷머리는 `r·0.99`·중심을 `-r·0.22` 로 물리고, 앞머리는 이마 위(`y + r·0.66`)에만 얹었다. 눈은 `HR·1.05`, 입은 `HR·1.03`, 눈썹·볼·코도 표면 밖으로. 어린이는 눈 1.72배·입꼬리 올린 웃는 입·얇은 눈썹·모자 밑 머리카락.
* **교통경찰 복장이 지역경찰 옷이었다(소유자 정정)**: 교통은 **상의 흰색 · 하의 남색**이다. `C.SHIRT_W` 를 넣고 상의를 흰색, 하의를 남색으로 바꿨다. 흰 상의가 밋밋해지지 않게 남색 옷깃과 견장(계급장 금색 바)을 넣었다. 차내 동승 경찰관도 흰 상의로. 등판 「교통경찰 POLICE」 는 그대로(맞다).
* **인트로 3컷을 다시 잡았다**: 제어함이 프레임에 없고 구도가 답답했다 → 북쪽에서 남쪽을 보며 경찰관 앞모습 + 열린 함체 + 뒤로 교차로. 문을 열고 수동에 넣은 상태로 시작하고, 새 몸짓 `'operate'`(팔을 앞으로 들어 스위치를 만진다, 신호봉은 넣는다)를 6.4~8.5초에 쓴다. 자막은 소유자 지시대로 「신호기 박스를 조작한다」.
* **한국어 자막이 단어 중간에서 끊겼다**(「걷 / 는다」) → `word-break: keep-all` 을 자막·안내·구간 표시에 적용하고 자막 폭을 760px 로 넓혔다.
* **어린이 컷이 너무 멀었다** → 화각 28°, 카메라를 아이 앞 4m 까지 붙였다.
* **어린이보호구역 표지·노면 표시가 간선(강남대로)에도 칠해져** 실제 규칙(`inSchoolZone`)과 어긋났다 → 표지·노면 표시도 같은 판정을 쓴다.
* 도시가 비어 보여 `TRAFFIC_MAX 18 → 26`, `PED_MAX 22 → 30`(프레임이 떨어지면 `TG.perf` 가 스폰을 줄인다).
* 확인한 것: 순찰 3인칭·뒷모습(후부 반사판 + 승강식 전광판 「사고 처리 중 ◀」)·추격전(먼지 흔적·HUD 2줄)·교차로 근무(제어함·조작판)·보행자·어린이(4단계 칩·표정)·학습 카드·결과 카드·일시정지 — 전부 정상. 콘솔 오류 0, IC 32/32, 홍보 0 오류.
  **한 줄짜리 JS 문장 뒤에 `//` 주석을 붙이면 그 줄의 나머지가 주석이 된다** — 이 판에서도 intro.js 가 이것으로 깨졌다(세 번째). 주석은 항상 줄 끝이나 다음 줄에.

## v0.9.7(2026-09-09) — 인트로 전체화면 · 어린이 교실 교육 장치(휙 지나가는 이륜차·함께 건너는 행인) · 법규 준수율 · 미니맵 확대

* **인트로 전체화면**(소유자 지시): 레터박스를 11vh → **6vh**(세로 4vh)로 줄여 화면을 채우고, 「⛶ 전체화면」 단추를 인트로(건너뛰기 왼쪽)와 타이틀 링크에 넣었다. 브라우저는 사용자 조작이 있어야 전체화면을 허용하므로 **인트로를 처음 터치할 때 자동으로 요청**한다(`G.goFull`). 다시 누르면 나온다.
* **어린이 교실: 왜 좌우를 봐야 하는지 보여 준다**(소유자 지시): 「👀 본다」 단계에 들어가면 좌우에서 자전거와 오토바이가 **휙 지나간다**(`kidZip`, 양쪽 바깥 차로에서 하나씩, 9.5m/s). 아이 9.5m 안을 지나가면 휘익 소리 + 「지금 오토바이가 휙 지나갔어요! 그래서 좌우를 보는 거예요」.
  **차량 신호가 녹색일 때만**(= 아이의 보행 신호가 적색일 때) 스폰한다 — 적색에 스폰하면 정지선에 서 버려 「휙」이 되지 않는다. 한 번 건널 때 한 번(30초 쿨다운).
* **함께 건너는 행인**(소유자: 「다른 행인도 있는게 좋지」): `kidCompanions` 가 3명을 배치한다 — 한 명은 **길 건너편에서 마주 건너오고** 나머지는 아이와 겹치지 않게 인도 위로 7m·12m 떨어져 선다.
* **법규 준수율**(소유자: 「교통법규를 너무 안 지키면 곤란해」): `VIOLATOR_RATE 0.30 → 0.12`, `BUSLANE_VIOLATOR_RATE 0.35 → 0.14`, 습관(휴대전화·꽁초 등) 0.22 → 0.10, 무면허 0.08 → 0.04, 자전거·PM 타고 건너기 0.5 → 0.28, 보도 통행 0.45/0.5 → 0.24/0.26, 건널목 돌파 0.7 → 0.35, 무단횡단 행인 0.18 → 0.07.
* **어린이를 더 어린이답게**: 걸음 빈도 4.6 → **6.6**(보폭이 짧다), 팔다리 흔들림 0.82배, 통통 튀는 상하 움직임 0.055(어른 0.035). 메시 해상도도 올렸다 — 얼굴 구 26×18, 팔다리 원기둥 16면·관절 구 12×9(어른은 그대로).
* **미니맵 확대**(소유자: 「너무 작아서 뭘 보는지 모르겠어」): 캔버스 236 → **360px**, 화면 크기 118 → **180px**(세로 96 → 148). 글자·선 두께·표시를 `K = W/236` 배율로 함께 키웠다(도로명 11px·역 이름 10.5px 기준).
* **오디오 방어**: `TG.audio.update` 가 non-finite 값을 받으면 Web Audio 가 예외를 던져 그 프레임이 죽는다 → 들어오는 값을 `isFinite` 로 걸러 0 으로 대체한다. 검증에 「수치 건전성(NaN 없음)」 항목을 넣었다.

## v0.9.8(2026-09-09) — 도로가 실제로 이어지게 다시 짰다 · 차가 노면에 묻히던 원인 · 보행자는 초록불에 건넌다

소유자 보고 6건을 모두 실측으로 원인까지 찾아 고쳤다. 「도로는 연결되어야 해」 · 「매끈하게 연결되어야 하는데 턱이 있고 차들이 도로에 반쯤 들어가서 달리고 있음」 ·
「일반도로에서 고속도로·자동차전용도로로 진입할 때 도로가 어긋나 있고 차들이 도로 위가 아닌 맨땅을 달리고 있어」 · 「도로 중간중간 차가 파묻혀」 ·
「차량이 도로에 묻혀서 달리고 있음」 · 「자동차전용도로를 잘 달리다 도로 속으로 들어가서 검은 화면이 나옴」 · 「보행자가 횡단보도 초록불일 때 건너야 하는데 그렇지 않아 무단횡단이 되는 거야」.

* **연결로와 램프 사이가 17~54m 벌어져 있었다(가장 큰 원인)**. 램프 원호의 시작점은 링 접속점 J 에서 `tc` 방향으로 56m 인데, 연결로는 `end`(= J − 방사방향 15m) 기준으로 56m 를 잘라서
  **둘이 15m(대각 IC 는 40~54m) 어긋나** 있었다. 그 사이에는 포장이 없어 차가 맨땅을 달렸다.
  → 연결로 분기점을 **J 기준**으로 잡고(`split = [J.x − Ept.tx·SPLIT, …]`), 램프 원호는 **연결로의 실제 끝 E 에서 시작**한다(`arc()` 의 T = E, 진입·진출이 같은 점에서 갈라진다).
  램프는 분기점보다 2.5m 뒤에서 시작하도록 잘라 포장이 겹치게 했다. 결과: **틈 17~54m → 0.6~4.1m(전부 포장 안)**.
* **폭이 절벽처럼 줄었다**: 왕복 8차로 경부고속도로(반폭 17m)가 반폭 5.8m 램프로 갑자기 바뀌어 이음부 양쪽에 맨땅이 드러났다.
  → `taperHalf(link, atStart, wide)` 로 램프의 연결로 쪽 44m 를 연결로 폭까지 넓힌다. 폭 차이 **11.2m → 0**.
* **합류부에 1~2.4m 턱이 있었다**: 링크마다 지형을 따로 완만화(`hb` 3패스)해 높이가 달랐다.
  → 램프 노면 중 **링 포장 안에 드는 구간은 링 높이 그대로**, 나머지는 분기점 높이까지 일정 기울기로 잇는다. 턱 **1~2.4m → 0.02m**.
* **`trimLink` 의 오래된 버그**: `link.pts[k].s -= link.pts[0].s` 를 루프 안에서 읽어 **첫 점만 0 이 되고 나머지 s 는 그대로** 남았다(`s` 가 0, 36, 40, 44…).
  `link.total` 도 틀렸다. 기준값을 먼저 떠 두도록 고쳤다. 위의 높이 잇기·폭 넓히기가 `s` 를 쓰기 때문에 이 버그가 먼저 드러났다.
* **차가 노면에 묻혀 달렸다 · 화면이 검게 나왔다**: `terrain.heightAt` 하나로 **지형을 깎는 높이**와 **차가 올라서는 높이**를 같이 쓰고 있었다.
  지형용은 겹치는 도로 중 **가장 낮은 노면**을 골라야 하는데(v0.9.2), 차량도 같은 값을 받아 **램프 위의 차가 아래 본선 높이로 내려앉았다**. 게다가 그 값은 노면보다 12cm 낮은 지형 높이여서
  평상시에도 바퀴가 아스팔트 아래에 잠겼다(실측 **−0.30m**). 카메라도 같은 값을 바닥으로 삼아 포장 밑으로 들어가 화면이 검게 나왔다.
  → 둘을 분리했다. `terrain.groundAt(x,z)`(지형·소품 = 가장 낮은 노면) / `terrain.heightAt(x, z, yHint)` = **`surfaceAt`**: 지금 높이 ±3m 안의 도로만 후보로 보고(고가/본선 구분),
  중심선에 가까운 도로를 골라 **포장 위(`p.y + 0.02`, 램프 0.04)** 를 준다. 후보가 없으면 층을 가리지 않고 다시, 그래도 없으면 `groundAt`.
  `city.heightAt(x, z, yHint)` · `vehicle.js` 4곳 · `traffic.js` · `walker.js` · `main.js` 카메라 2곳이 지금 높이를 넘긴다. 실측 **차량 높이 − 포장 높이 = 0.000m**.
* **노면 옆치마(4m 벽)가 차로 가운데를 가로지르는 「턱」으로 보였다**: 램프가 본선 포장 위를 나란히 달리는 구간에서도 벽을 세우고 있었다.
  → 벽은 **다른 도로 포장이 덮고 있지 않고, 실제로 지형이 0.5m 이상 꺼진 곳**에만 세운다.
* **방호벽·난간이 램프를 막아 차가 멈췄다(onN STUCK)**: `rampGap` 의 탐색 반경 18m·측방 띠 9.5~24m 가 좁아, 원호에서 가속차로로 바뀌는 구간(측방 13~19m)과
  분기점 바로 앞(측방 0~7m)에서 난간이 남았다. → 반경 26m, 측방 1.5~28m 로 넓혔다(실제 IC 도 합류·분기부에 방호벽이 끊긴다).
* **연습 서킷이 경부고속도로·양재IC 램프를 22m 파고들고 있었다**. 소유자 사진 1의 왼쪽 적·백 연석이 바로 이 서킷 코너 연석이다.
  `frameAt` 이 고속도로 위를 「연습 서킷(반폭 7.5m) 밖」으로 잡아 **도로 위인데 도로 밖**으로 판정됐다.
  → 서킷을 **x +460m**(동쪽 해안 평지)로 옮겼다. 가장 가까운 도로 끝에서 **72.9m**, 고저차 1.5m. 링크 연결이 없으므로 AI·주행 경로에는 영향이 없다.
* **연결로 이름·제한속도가 주석에 먹혀 있었다(같은 사고 네 번째)**: 한 줄 주석 뒤에 문장을 이어 붙여 `conns[0]·[1]·[3]~[7]` 의 `name`·`limit` 이 전부 주석 처리돼 있었다.
  그래서 HUD 가 「램프 · 제한 80」·「교외 도로」로만 나왔다. 줄을 나눠 고쳤다. **주석은 항상 줄 끝이나 다음 줄에.**
* **보행자가 적색·점멸에 건넜다**: ① 교차로에 닿으면 절반만 대기 상태로 들어가고 나머지는 그냥 지나쳐 횡단보도에 들어갔다. ② 판정이 `pedWalk` 하나뿐이라
  **녹색 점멸(마지막 3초)에도 횡단을 시작**했다(시행규칙 별표2 위반).
  → 건널 사람은 연석에서 기다리고(62%, 한 주기 62초까지 기다린다), **`pedRemain ≥ 3.2초` 일 때만 출발**한다. 건너는 중에 점멸·적색으로 바뀌면 되돌아오지 않고 **걸음을 재촉**한다(별표2: 신속히 횡단 완료).
  무단횡단(7%)만 예외로 남겨 단속 소재로 쓴다. 검증: 60초 동안 **횡단 시작 11회 · 적색·점멸 출발 0회**.
* 상시 검증 추가: `_verify_tail.js` **5t** 보행자 횡단 시작 신호 준수(위반 0 + 실제로 건넌다) · **5u** 연결로↔램프 이음(최대 틈/턱/폭차 + 램프 위 노면 높이).

### v0.9.8 두 번째 묶음 — 소유자가 실제로 달려 보며 지적한 「연결성」 결함 전부

* **도시 도로가 연결로로 한 칸에 좁아졌다**(소유자: 「연결부 도로가 점차 줄어들어야 함. 너무 성의 없이 만들어짐」):
  왕복 8차로 강남대로(반폭 17m) 끝에서 반폭 6m 연결로가 바로 시작했다. → `taperHalf(conn, true, city.halfOf(axis, idx), 90)` 로 **90m 에 걸쳐 17 → 6m** 로 줄인다(변속차로 모양).
  2차로 도로의 가장자리 실선도 고정 3.63/3.77 대신 `half − 2.37/2.23` 로 **실제 폭을 따라간다**(넓어진 구간에서 차로 가운데에 선이 그려졌다).
* **한남대교 연결로가 강 한복판에서 끊겼다**(「도로가 또 끊어져 있음」): 분기점이 링 접속점 56m 뒤 = 한강 위였다. 두 램프와 분기점이 물 위에 조각으로 떠 있었다.
  → 분기점을 **다리 위에 두지 않는다**: 링에서 멀어지는 쪽으로 6m 씩 물러나며 `river(x,z) > -1.2` 인 첫 지점을 고른다(N 은 56 → 110m). 강은 두 램프가 나란한 다리로 건넌다.
* **램프가 「말뚝」으로 막혀 있었다**(「진입로도 끊겨있고 말뚝으로 막혀있음」): 방호벽(`wallQuad`)·충돌벽(`walls`)은 `rampGap` 으로 비웠는데 **지주(0.12×0.8×0.12 박스)는 무조건 세우고 있었다**.
  → `barOk` 한 변수로 벽·지주·충돌을 함께 판단한다. 다리 난간 지주도 같게.
* **분기 노즈에 난간이 남았다**: `rampGap` 이 `lat` 만 보다가, 멀리 앞쪽 램프 점의 **종방향 성분이 섞여 부호가 뒤집혔다**. → `alo`(종방향)를 함께 계산해 `|alo| ≤ 12` 인 나란한 구간만 본다.
  거기에 `conn.splitEnd` 를 세워 **분기점 앞 46m 는 난간·방호벽을 아예 두지 않는다**(실제 IC 의 분기 노즈).
* **노면을 가로지르는 흰 띠 3줄**(「도로 앞에 의미 없는 가로 3줄이 있음」): 램프 코 부분 「도류선」을 폭 전체를 채운 띠로 그리고 있었다. → 없앴다.
* **램프 차로선이 본선 차로까지 이어져 보였다**(「연결도로선이 본선도로까지 이어짐」): 가속·감속차로에서 램프의 가장자리선을 본선 위에 겹쳐 그렸다.
  → 격자 완성 뒤 램프 점마다 `p.inMain`(본선 포장 안인지)을 표시하고, 그 구간에서는 램프 가장자리선을 **그리지 않는다**(본선 표시가 기준).
* **가속차로에서 「램프 · 제한 80」으로 나왔다**(「속도가 잘못 계산이 됨」): `frameAt` 이 더 가까운 램프를 골랐다. → 램프 위라도 **본선 포장 안이면 본선 프레임**을 쓴다
  (`nearest(x, z, 'no')` = 램프 제외 모드 추가). 이름도 `onramp` → 「진입로(램프)」 · `offramp` → 「진출로(램프)」 로 구분했다.
* **선을 넘을 때 「쿵」 하고 걸렸다**: `surfaceAt` 이 램프는 `+0.04`, 본선은 `+0.02` 를 줘서 램프 포장에 올라서는 순간 2cm 계단이 생겼다.
  → 물리 노면 높이는 **도로 종류와 무관하게 `+0.02`** 로 통일한다(포장을 2cm 높이 그리는 것은 겹침 방지용 시각 처리일 뿐이다). 램프 종단선형에 `smoothY`(3패스, 양 끝 고정)도 걸었다.
* **나란히 가는 램프와 본선 사이가 절벽이었다**(「연결도로 부분이 단차로 막혀 있음. 옆쪽으로 길이 나란히 가다가 접속이 되어야 함에도 그냥 가져다 붙인 흔적임」):
  `groundAt` 이 **도로 하나를 골라** 지형을 그 높이로 깎아, 높이가 다른 두 도로 사이에 계단이 생겼다.
  → `roadCands(x,z)`(도로마다 가장 가까운 한 점)를 모아 **가장 낮은 노면 +4m 안의 노면들을 거리 가중 평균**한다. 4m 이상 높은 것은 고가이므로 무시한다(본선이 흙에 묻히던 v0.9.2 규칙 유지).
  실측: 램프 ↔ 본선 지형 단면 2m 당 최대 단차 **0.01m**.
* **`trimLink` 가 되돌아 꺾인 점을 남겼다**: 한남대교 연결로에 간격 0.81m(정상 4m) 지점이 있어 그 자리에서 노면 사각형이 뒤집혀 **도로가 끊긴 것처럼** 보였다.
  → 자른 뒤 앞 점과 `STEP × 0.6` 미만인 점을 버리고 거리·접선을 실제 좌표로 다시 낸다. 전 링크 검사: 꺾임 **1곳 → 0곳**.
* **순찰차 도색 글자가 좌우로 뒤집혀 있었다**(「폴리스와 경찰 글씨가 꺼꾸로 써져 있음」): 양쪽 다 뒤집혀 있었다 — `liverySide(flip)` 을 좌우 서로 바꿨다. 이제 양쪽에서 「경찰 POLICE」로 읽힌다.
* **도로명 변경**(소유자 지시: 「순환도로를 경부고속도로로 이름을 바꾸자. 윗부분은 올림픽대로로」): `frameAt` 의 고속도로 이름이 `z < -120` 이면 **올림픽대로**, 그 밖은 **경부고속도로**.
  미니맵 범례·안내표지(`gantry`)·인트로 자막·자유 주행 안내도 맞췄다. 버스전용차로는 남쪽 연결로(`link.busLane`)에만 있으므로 링 표지에서 버스전용 문구를 뺐다.

### v0.9.9 — 인트로도 「이어진 길」을 보여 준다

* 소유자: 「위와 같은 개념으로 인트로 화면 도로 연결부분을 연결하고 자연스러운 현실세계를 보여줘야 함」.
* 여는 항공 샷을 **남쪽 IC(경부고속도로가 순환 본선에 붙는 곳)** 위에서 시작해 도시로 내려오게 바꿨다 — 첫 3.6초가 「길이 이어져 있다」를 먼저 보여 준다.
* **살아 있는 도시**: 인트로 시작 때 도시 전체에 차 26대·행인 16명을 미리 깐다(`seedCity`). 평소 스폰은 플레이어 주변에서만 일어나 항공 샷이 텅 비어 보였다.
* 검증: verify ERRORS 0(인트로 7항목 포함) · promo 0 · 포장 구멍 0 · IC 32/32.

### v0.9.10 — 흙에 파묻히던 원인 · 길 위의 시설물 · 지정차로 · 판 번호 표시

* **차가 흙에 파묻혀 전진도 후진도 못 했다**(소유자: 「순찰차가 땅속으로 들어갔어 … 후진도 안되고」): `groundAt` 의 거리 가중 평균이
  옆에 있는 **더 높은 경사로**를 함께 평균해 지형을 노면 위로 끌어올렸다(실측 +0.45m). → 이 자리를 덮는 노면과 가장 가까운 노면 **아래로 반드시 내린다**.
  전 링크 주행 폭 전수 검사: 파묻힘 **26곳 → 0곳**.
* **표지 기둥이 차로 한가운데 서 있었다**(「도로 한가운데 … 도로표지판이 있어서 사고를 내려고 하고 있음」): 램프가 `taperHalf` 로 넓어지면서 본선 표지를 덮었다.
  → `clearSpot()` 이 다른 도로의 포장 밖으로 3m 씩 밀어내고, 끝내 자리가 없으면 세우지 않는다. 제한속도 표지·갠트리 다리 모두. 길을 막는 충돌벽 **0**.
* **램프 차선이 대각으로 「V」 자를 그렸다**(「차선이 대각으로 그려져 있는 부분 전부 없애고」): 넓어진 이음부에서도 가장자리선을 그려 폭 변화가 선으로 드러났다 → 그 구간은 긋지 않는다.
* **신호등 기둥이 보도 한가운데**(「횡단보도 진행 방향에 저런 시설물은 없어」 · 「보행자 신호등 있는 곳에 같이」):
  차량 신호등 기둥을 차도 가장자리 +1.6m 에서 **보행 신호등과 같은 자리**(`city.sideOff + 1.0`, 보도 바깥선)로 옮겼다. 암은 그만큼 길어진다. 보행 신호등 기둥도 같은 선으로.
* **지정차로**(소유자: 「경부고속도로 버스전용차로에서는 그냥 다 주행만 하고, 올림픽대로에서는 지정차로 위반 — 1차로는 진입할 수 없음. 승합차 화물차는 가운데 기준 우측차로로만」):
  버스전용차로가 **있는 도로(경부고속도로)에서만** 버스가 1차로를 쓴다. 그 밖의 고속도로에서는 승합·화물이 1차로에 들어가지 않고 오른쪽 차로로 통행한다(`laneFor`·링크 스폰 모두).
* **어린이 보행 교실**: ① 손을 들고 들어섰으면 **다 건널 때까지 손을 든 채로**(전에는 4초 뒤 내려갔다 — 소유자 지적). ② 신호를 기다리는 사람은 연석보다 1.6m 뒤,
  **보도 위**에서 기다린다(횡단보도 띠 위에 서 있으면 위험하다).
* **이륜차·자전거·PM 대응**(소유자: 「이륜차 횡단보도 통행은 위반이지만 안전이 우선입니다」): 등급 C 안내를 「단순 위반 · 추격 금지 → 📹 영상 · 📡 무전 **(위반이지만 안전이 우선)**」로,
  `laws.json` bikeCross 의 현장 요령에 「세우려다 넘어지면 더 크게 다친다」를 적었다(법령 수치는 건드리지 않았다).
* **판 번호를 화면에 찍는다**: `TG.VERSION` 을 타이틀(`#verTag`)과 콘솔에 표시. 옛 판을 보면서 고쳐진 것을 못 보는 일을 없애기 위해서다.
  `sw.js` 의 미리 저장 목록이 `?v=0.9.7` 에 멈춰 있던 것도 `CACHE` 에서 뽑아 쓰게 고쳤다.

### v0.9.11 — 지도를 서초구로 · 경찰 표장 다시 그리기 · 실물 이미지 자리 마련

* **서초구 축약 지도**(소유자: 「서초구 위주로 지명을 만들어주고」). 남북 서→동 = 동작대로 · 방배로 · **반포대로** · 서초중앙로 · **강남대로**(구 경계),
  동서 북→남 = 신반포로 · 사평대로 · **서초대로** · 효령로 · **남부순환로**. `LANES_V [4,2,4,2,4]`(동작·반포·강남대로 왕복 8차로).
  교차로 이름 15곳(이수·사당역·방배역·반포·고속터미널·**서울성모병원**·서초3동·예술의전당·서초역·서초구청·남부터미널·신논현역·강남역·양재역).
  랜드마크: 고속버스터미널·반포종합운동장·서울중앙지방법원·예술의전당·서초구청·강남대로 업무타워·아파트 타워.
  IC 8곳도 서초 기준(강남·반포·경부고속도로 시점·사당·신논현·동작대교·양재·방배). `hName` 의 테헤란로 분기는 없앴다(지도가 서초구 안에서 끝난다).
  **교차로 근무·인트로 무대는 `nodes[2][2]` 서울성모병원 사거리**로 옮겼다(전 `nodes[0][1]`). `signals.NODE_MIN` 키도 새 인덱스로.
* **경찰 표장을 다시 그렸다**(소유자: 「경찰 마크 이미지는 이걸로 써라」): 전 판 방패 마크에는 **무궁화가 아예 없었다**.
  이제 금테 방패 · 남색 바탕 · 참수리(날개 끝이 위로 벌어짐) · **저울** · 무궁화 + 태극 · 「경」「찰」 · 「POLICE」 로 실물 구성을 따른다.
* **실물 이미지를 그대로 쓸 자리**: `js/emblem.js` 의 `TG.EMBLEM = { shield, eagle }` 에 **데이터 URI**를 넣으면 `textures.js` 가 캔버스 그림 대신 그 이미지를 쓴다.
  파일을 따로 두지 않고 문자열로 넣는 이유는 「외부 이미지 파일 0개 · 네트워크 요청 0」 규칙을 지키면서 오프라인·file:// 에서도 뜨게 하기 위해서다. 값이 null 이면 지금처럼 그린다.
* **추격 음악이 프레임을 죽이던 버그**: `chaseTension(NaN)` → `Math.min/max` 로는 NaN 이 걸러지지 않아 Web Audio 가 예외를 던졌다. `isFinite` 가드를 오디오와 chase 양쪽에 넣었다.

### v0.9.12 — 검은 화면의 진짜 원인(NaN 전파) · 중앙분리대 진입부 · 주행 소크 검사

* **화면이 검게 나오던 진짜 원인을 잡았다.** 순서는 이렇다.
  ① `traffic.js` 에서 이륜차·자전거·PM 을 만들 때 **한 줄 주석이 뒤 문장 두 개를 먹어** `car.edgeOff`·`car.edgeT` 가 `undefined` 였다(같은 사고 **다섯 번째**).
  ② 그 값이 계산에 섞여 그 차의 `heading`·좌표가 **NaN** 이 됐다.
  ③ 플레이어 ↔ 차량 접촉 판정은 `if (d2 >= rr*rr || d2 < 1e-6) continue;` — **NaN 은 두 비교 모두 거짓**이라 걸러지지 않고 통과해
     `player.pos.x -= NaN` 으로 **플레이어 좌표까지 NaN** 이 됐다. 그 순간부터 화면이 검게 나오고 조작이 먹지 않는다.
  → 세 겹으로 고쳤다: 주석에 먹힌 두 줄 복구 · 접촉 판정을 **긍정형**(`!(d2 < rr*rr) || !(d2 >= 1e-6)`)으로 바꿔 NaN 이면 반드시 건너뛰게 · `traffic.sweepNaN()` 이 좌표가 깨진 차를 매 프레임 치운다(차량 간 밀어내기도 같은 방어).
  실측: 3분 주행에서 NaN 차량 3대·플레이어 NaN 발생 → **0 / 0**.
* **중앙분리대가 도시 진입부에서 갑자기 시작했다**: 도시 도로에는 벽이 없다가 연결로 첫 지점부터 충돌벽이 서서, 차로를 바꾸던 차가 없던 벽에 부딪혔다.
  → 도시 진입 **70m**와 램프 분기부에는 분리대를 세우지 않고 **황색 복선**만 긋는다.
* **주행 소크 검사를 상시 검증에 넣었다**(`_verify_tail.js` 5v): 도시·본선·연결로를 약 3분 자동 주행하며 **NaN 차량 0 · NaN 플레이어 0** 을 확인한다.
  이 검사가 있었으면 위 버그는 처음부터 잡혔다.
* **한 줄 주석 뒤에 문장을 이어 붙이지 않는다** — 이 프로젝트에서만 다섯 번 사고가 났다(traffic 2회 · main · intro · peds · terrain). 주석은 **줄 끝이거나 다음 줄**이다.

### v0.9.13 — 경찰 표장 실물 이미지 · 인트로 1인칭/3인칭 · 보행 녹색 시간을 폭에 맞춰 · 한강·양재천 · 어린이 안전

* **경찰 표장은 소유자가 준 실물 이미지를 그대로 쓴다**: `ref/shield.png`(방패 · 순찰차용) · `ref/eagle.png`(참수리)를 base64 로 `js/emblem.js` 에 심었다(235KB).
  파일을 따로 두지 않으므로 「외부 이미지 파일 0개 · 네트워크 요청 0」 규칙은 그대로다 — 오프라인·file:// 에서도 뜬다.
  흰 바탕은 `textures.js` 의 `cutWhite()` 가 **가장자리에서 이어진 흰색만** 지운다(안쪽 흰 글씨 「POLICE」 는 남는다) → 경계로 자르고 정사각 텍스처로 만든다. 교통안전 홍보·공익 목적(소유자 확인).
* **인트로를 1인칭·3인칭으로 다시 짰다**(17.2초 → **26.4초**): 항공(조문) → **1인칭 운전석**(핸들·계기판) → 3인칭 위반 발견 → 교차로 근무 → 어린이 →
  **타이틀 12.6초** → **1인칭 순찰**「서울을 순찰한다」 → 3인칭 광각「근무 연습 · 스트레스 해소 · 교통안전 홍보」 → 크레인 아웃 → 정착.
  샷에 `view: 'cockpit'` 을 주면 `player.setView` 와 근접 평면(0.12)을 함께 바꾼다. 타이틀은 14.3초에 물러나 뒤 샷을 덮지 않는다.
  자막을 크게 했다(ttl 22~42px → **30~62px**, 조문 18~30 → **23~42**). 인트로 동안만 안개를 물려 도시가 또렷하게 보인다.
* **보행 녹색 시간을 도로 폭에 맞췄다**(소유자: 「어린이가 건널 수 있는 충분한 시간이 필요해. 중간에 빨간불로 변하고 있어」):
  `signals.pedTime(node, crossAxis)` = **진입 5초 + 횡단거리 ÷ 1.15m/s**(어린이 걸음), 차량 녹색보다 2초 짧게 묶는다. `SIG_GREEN 24 → 30`.
  왕복 4차로 25초 · 6차로 28초 · 8차로 28초(상한). 수동 조작의 「보행 최소」도 그 횡단보도의 실제 시간을 쓴다.
* **횡단보도에 사람이 있으면 다 건널 때까지 차량이 선다**(제27조 · 소유자 지시): `peds.onCrossing(node, d)` 로 그 접근로의 횡단보도 위를 보고 정지선 앞에 세운다.
  앞만 보는 판정(`nearestAhead`)으로는 옆 차로로 건너오는 사람을 놓쳤다. **연석에 서 있는 사람은 세지 않는다**(`|lat| < half + 0.3`) — 그러면 통행이 아예 멈추고 「휙 지나가는 이륜차」 수업도 죽는다.
* **어린이는 횡단보도 위에서 끝까지 손을 든다**(조건 없이 유지).
* **신호등 기둥을 줄였다**(소유자: 「폴대를 합쳐서 화면 가림을 최대한 억제」): 보행 신호등을 **횡단 방향 오른쪽 한 곳**에만 세운다. 교차로당 기둥 12개 → **8개**.
  4색 신호등 기둥은 네 접근로 모두 **이미 진행 방향 우측**(측방 +16~19.5m)임을 실측으로 확인했다.
* **한강을 넓혔다**(68m → **124m**), **양재천**도 또렷하게(18m → 26m, 산책로 ±15.5m). 한강 쪽 호는 올림픽대로, 나머지는 경부고속도로.
  물 위 도로는 자동으로 다리가 된다 — **다리 판정을 물 판정과 같은 기준으로** 바꿨다(전에는 `river < -1.2` 만 봐서 물에는 잠기는데 다리가 아닌 구간이 18곳 생겼다).
  **물 복귀는 다리 상판 위에서 하지 않는다**(가장자리 3m 유예) — 다리가 길어지자 차로를 살짝 벗어날 때마다 뒤로 순간이동시키는 반복이 생겼다.
* **철길건널목을 제대로 만들었다**: 선로가 ±90m 에서 끊기고 도로 위에서 레일이 사라졌다 → **±220m**, 자갈 도상(둑) + 침목 + 레일 2줄, 도로 구간은 건널목 판을 깔고 **레일을 끊지 않는다**.
* **램프가 본선 위에 겹쳐 그려지던 것**을 없앴다(소유자: 「연결부분이 본선까지 쭉 들어가서 장애물이 되고 있어 맨 바깥」): 본선 포장 안(`p.inMain`)에서는 램프의 포장과 옆치마를 그리지 않는다.

### v0.9.14 — 교통안전교실 ① 「깜빡이는 초록불」 · 어린이 별점이 0 이던 버그

* **어린이 별점이 언제나 0 이었다(여섯 번째 주석 사고)**: `var st = TG.clamp(...);   // 주석 walk.stars += st; walk.crossings++; addScore(st * 10, null);`
  — 한 줄 주석 뒤에 문장 셋을 이어 붙여 **적립이 통째로 주석 처리**돼 있었다. 아무리 잘 건너도 별이 쌓이지 않았다.
  고친 뒤 실측: 별 **0 → 3**, 점수 5 → 35. **주석은 줄 끝이거나 다음 줄.**
* **교통안전교실 ① 「깜빡이는 초록불, 급한데 그냥 뛰어가도 괜찮을까?」**(소유자가 준 한국교통안전공단 교재 목록의 4화):
  근거는 **시행규칙 별표2** — 보행신호등 녹색등화의 점멸에는 **횡단을 시작할 수 없고**, 이미 건너는 사람은 신속히 건너거나 되돌아온다.
  - **연석에서 기다릴 때** 녹색이 점멸이면 단계 칩이 「🟡 깜빡여요 — 기다린다」로 바뀌고 음성·안내가 나온다. 기다린 사실을 기억해 둔다(`walk.blinkWaited`).
  - **점멸에 들어서면**: 어른은 `walkBlink` **−6점**(별표2 인용), 어린이는 **감점 없이 가르치고** 그 횡단의 별을 하나 뺀다(`walk.cross.blink`).
  - **기다렸다가 다음 초록불에 건너면** 「👏 깜빡일 때 기다렸다가 건넜어요 (+10)」.
  - 어린이 음성 `blink` 3종 추가(같은 뜻을 돌려 쓴다).
* `wk`(보행 녹색)·`rem`(잔여)을 단계 분기보다 **먼저** 구하도록 정리했다(전에는 뒤에서 선언해 깜빡임 분기가 `undefined` 를 봤다).
* 상시 검증 **5w**: 점멸 잔여 2.6초에 어른이 들어서면 −6, 어린이는 0점 + 깜빡임 표시 + 안내 문구까지 확인한다.

### v0.9.15 — 교통안전교실 ② 자전거·킥보드(초등 / 중고등)

* 소유자 지시: 「자전거 킥보드 초등학생과 중고등생을 위해서 만들자. 어떤 건 면허가 있어야 하고 어떤 건 그냥 타도 되고,
  타고 다닐 때는 도로교통법을 지켜야 하고, 발로 미는 킥보드도 조심해서 타고, 자전거도 잘 타고 잘 관리하는 법을. 안전이 중요하니까」.
* **근거는 전부 티북 v21.94 에서 grep 해 옮겼다**(기억으로 숫자를 쓰지 않는다). `data/laws.json` 에 새 섹션 **`rideSafe`** 12항목:
  - **면허**: 개인형 이동장치 = 최고속도 25km/h 미만 · 총중량 30kg 미만(제2조 제19호의2), **원동기장치자전거면허 이상 · 만 16세 이상**(제80조·제82조).
    자전거와 발로 미는 킥보드는 면허가 없어도 탄다. PM 무면허·음주는 자동차(제152조)와 달리 **범칙금 통고처분**(제156조).
  - **13세 미만 PM 운전**: 어린이는 처벌하지 않고 **보호자 과태료**(제11조 제4항 · 별표6, 티북 기재 10만원).
  - **안전모**(제50조 제3항·제4항 · 별표8, PM 미착용 2만원) · **2인 탑승**(제50조 제4항, 4만원) · **야간 등화**(제50조 제9항, 1만원).
  - **통행 위치**: 자전거는 「차」(제2조 제17호). 자전거도로가 없으면 **차도 우측 가장자리**. 보도 주행 3만원 —
    예외 **65세 이상 · 13세 미만 · 신체장애인 · 도로 파손·공사**. → 초등학생은 보도로 다녀도 된다는 점을 그대로 가르친다.
  - **횡단보도는 내려서 끌고 간다**(제13조의2 제6항). 타고 건너면 보행자가 아니어서 12대 중과실이 성립하지 않는다.
  - **앞에 사람·차가 있으면 기다린다**(제27조) · **타기 전 점검**(브레이크·체인·공기압) · **몸에 맞는 자전거**(발 앞부분 반이 땅에 닿는 크기).
  - **발로 미는 킥보드**: 동력이 없어 면허 불필요. 다만 「도로에서 어린이가 위험한 놀이기구를 타지 않도록」 하는 보호자 의무 조항은
    **티북 v21.94 에 기재가 없어 대조하지 못했다** → `verified: false`, 화면에 「확인 중」으로 나온다. 안전 수칙만 가르친다. **소유자가 원문으로 확인해 채운다.**
* `js/study.js` 가 학습 카드 패널에 두 번째 묶음으로 렌더한다 — **🧒 초등학생(3) · 🎒 중·고등학생(2) · 🚲 공통(7)**.
  숫자는 여전히 코드에 없다(laws.json 만 읽는다). 카드 24장(12대 중과실 12 + 자전거·킥보드 12), 「확인 중」 1건.
* 상시 검증: `자전거·킥보드 카드 = [12, 초등 3, 중고등 2, 공통 7, 묶음 4, 확인중 1]`.

### v0.9.16 — 신호등 기둥을 정지선 옆 하나로 · 교통안전교실 ③ 어린이보호구역 · 자전거·킥보드 시연

* **기둥 하나에 신호등을 모았다**(소유자: 「보행신호폴과 차량신호폴을 하나로 합치고 … 가로등은 삭제하고 신호등만, 위치할 곳은 정지선 바로 옆으로」):
  접근로마다 기둥 **1개**를 `정지선 + 0.9m`, 진행 방향 **오른쪽 보도**(`sideOff + 1.0`)에 세운다. 높이 6.4m. **가로등은 달지 않는다.**
  - 그 기둥의 **차량 신호등 머리는 반대편(맞은편에서 오는) 운전자가 읽는다** — 그래서 정지선 옆에 두고도 보인다.
  - **보행 신호등 머리는 같은 기둥에 90도로 두 개** — 두 방향 횡단보도를 한 기둥이 담당한다.
  - 실측: 교차로 1곳당 **기둥 4 · 차량등 4 · 보행등 8**. `city.lamps` 는 독립 가로등 150개로 되돌렸다(교차로 회피 18m).
* **어린이보호구역이 도로에 실제로 깔려 있는지 실측했다**(소유자 지시): 학교 블록(1,2) 둘레 **x 70~146 · z 174~250**,
  구역 칸 **594개 전부 제한속도 30**, 「어린이보호구역」 노면 표시 4곳, 학교 표지 4 + 30 표지 4.
* **교통안전교실 ③ 어린이보호구역** — `data/laws.json` 새 섹션 `schoolZone` **8항목**(전부 티북 v21.94 에서 grep):
  §12① 30km/h·어린이는 13세 미만 · §30 1호(**순찰차·소방차도 특례 없음**) · 특가법 §5의13(민식이법)·교특법 §3②11 ·
  별표10 **범칙금·벌점 2배(08~20시)** · §27 횡단보도 일시정지(별표8 11호, 승용 6만·벌점 10, 2026.4.20 우회전 집중단속) ·
  별표7 3호 주정차(승용 12만(13만)·승합 13만(14만), **이륜 금액은 티북에 없어 「확인 중」**) · 어린이 행동요령 · 운전자 행동요령.
* **말로 하지 않고 게임으로 시연한다**(소유자: 「말로만 하지 말고 게임 움직임으로 자연스럽게」):
  학습 카드의 항목에 **「🚓 게임에서 시연」** 단추가 붙고, 누르면 그 장면으로 바로 들어간다.
  - `schoolzone` — 방배로 어린이보호구역 안, 제한 30, 어린이 3명이 손을 들고 건넌다.
  - `bikecross` — 같은 횡단보도에서 **자전거는 내려서 끌고**, **킥보드는 타고 건넌다**(위반). 나란히 비교된다.
* **시연 장면을 깨끗하게 치운다**: `bikecross` 진입 때 주변 이륜차·자전거·킥보드와 **교차로 34m 안의 차를 먼저 지운다**
  (전에는 지나가던 킥보드가 섞여 무엇을 보라는 건지 알 수 없었다). 실측 `[자전거 1 · 타고 건너는 킥보드 1 · 주변 차 0]`.
  두 사람에게 `edgeRider` 를 켜야 「횡단보도 앞에서 내려 끈다」 규칙이 돈다 — 켜지 않아 **끌고 걷는 동작이 아예 나오지 않던 것**을 고쳤다.
  대신 `edgeT = -30` 으로 두어 시연 중에 보도 주행 위반이 먼저 뜨지 않게 했다(배울 것은 횡단보도 승차 통행 하나다).
* **상시 검증 5t 가 표본 0 을 내던 것**을 고쳤다. 두 겹이었다.
  ① 앞선 서킷 자동주행이 남긴 입력 덮어쓰기가 그대로 살아 있어 순찰차가 계속 굴러갔다 → 행인이 130m 밖으로 밀려 전부 despawn.
  ② 그래서 넣은 「제동 1」이 더 나빴다 — **정지 상태에서 제동을 누른 채로 두면 후진(R)** 으로 바뀌어 뒤로 16km/h 로 기어갔다(게임 사양대로다).
  → `T.override(null)` 로 입력을 비운다. 실측 **횡단 시작 13~14건, 적색·점멸 출발 0건, 최소 잔여 18.4초**.
* 검증: `_verify` ERRORS(0) 2회 · IC 8/8 도달 · 램프가 본선 벽 띠 안으로 들어간 점 0 · 3분 주행 소크 NaN 0 · 프로모 ERRORS(0).

### v0.9.17 — 모든 모드를 직접 몰아 보고 고친 것 (노면 세로쓰기 · 버스전용차로 복선 · 앞바퀴 조향 · 후진 · 화면)

소유자 지시: 「니가 직접 주행해봐 모든 모드로 … 화면도 점검하고」 · 「방향전환 커브시 앞타이어의 움직임이 고장난것 처럼 보여,
모든 도로에서 후진도 가능하게 하고, 버스전용차로 글씨를 제대로 써야 함, 파란색 실선이 1차로를 확보해줘야하는데」 ·
「용전스버가 아니라 세로쓰기를 제대로 해야함 위에서부터 읽어 버스전용이라고. 어린이보호구역도 마찬가지」.

* **노면 세로쓰기가 거꾸로였다**: `TG.tex.roadText()` 가 첫 글자를 캔버스 **아래쪽**에 그려, 운전석에서 위에서부터 읽으면 「용전스버」가 됐다.
  쿼드는 진행 방향 +180도로 놓여 v=0 이 가까운 쪽이므로, 첫 글자를 캔버스 **위쪽**(`128 * i + 66`)에 그리도록 한 줄 고쳤다.
  **글자 방향은 그대로**(바로 선 상태) 유지된다. 이 한 곳으로 「버스전용」·「어린이보호구역」·「천천히」·노면 도로명이 전부 바로잡혔다.
  실측: 운전석에서 위에서부터 **버스전용** · **어린이보호구역** 으로 읽힌다.
* **버스전용차로 경계를 청색 복선으로**: 전에는 단선 1줄이라 1차로가 「확보」돼 보이지 않았다. 이제 `hb−0.42~−0.22` 와 `hb+0.08~+0.28`
  두 줄을 그린다. 1차로(중앙선~3.75m)가 노란 중앙선과 청색 복선 사이에 또렷하게 남는다. 경부고속도로(`link.busLane`)만.
* **앞바퀴가 고장난 것처럼 비틀리며 돌던 것**(소유자 지적): 바퀴 메시의 회전 순서가 기본값 `XYZ` 라 **조향(Y)이 구름(X) 축에 끌려갔다**.
  `wh.rotation.order = 'YXZ'` — 제자리에서 굴러가는 바퀴를 수직축으로 돌리는 순서다. 앞 두 바퀴만 조향한다(그대로).
* **후진**: 실측해 보니 시내 8·4차로 · 경부고속도로 · 램프 · 서킷 · 다리 · 방호벽 옆 · 모든 주행 모드에서 **이미 후진이 걸린다**(16km/h).
  다만 **도로 밖**(잔디·공원)에서는 저항 0.9 때문에 10km/h 로 느렸다 → 후진일 때만 저항을 0.30 으로 낮췄다. 실측 4초에 8m → **13m**.
  하차 근무(교차로 근무)만 후진이 없다 — 차에서 내려 있기 때문이다.
* **표지판 뒷면에 글씨가 거울처럼 비쳤다**: 방향마다 면을 따로 세우는데 재질이 `DoubleSide` 였다 → 단면으로 바꿨다.
* 화면 점검에서 고친 것(폰 가로 844×390 실측):
  - 연습 서킷 구간 표시 「제한 999」 → **「제한 없음」**.
  - 「남은 시간」 칸에 랩 타임이 들어가 라벨이 어긋났다 → 라벨을 모드별로(**랩 타임 · 주행 · 남은 시간**).
  - 도보 근무(보행·어린이·교차로)에서 기어 배지 **N** 이 그대로 떠 있었다 → 감춘다(도보에는 기어가 없다).
  - 추격전 구간 문구가 길어 속도 카드가 화면 절반(500px)을 덮었다 → 카드 폭 `min(42vw,340px)`, 실측 340px.
  - 어린이 교실의 **✋ 손 들기 단추가 미니맵을 덮었다** → 폰 가로에서는 미니맵 왼쪽 옆으로 옮긴다(실측 겹침 0).
  - **인트로 글씨가 위로 잘렸다**(390px 높이에서 블록이 397px) → 글자 크기는 그대로 두고 줄 간격·아래 여백만 줄여 **90~380px 안**에 들어온다.
  - 타이틀 단추 「학습 12항목」 → **「학습·교통안전교실」**, 학습 카드 머리 문구도 세 묶음(12대 중과실·어린이보호구역·자전거·킥보드)으로.
* **직접 주행 결과**(브라우저에서 실제 조작·시뮬레이션): 순찰 50초 · 자유 35초 · 서킷 90초 · 교차로 근무 12초 · 추격전 30초 ·
  보행자 20초 · 어린이 15초 + 학습 카드 · 인트로 · 시연 2종. **콘솔 오류 0 · NaN 0 · 도로 이탈 0 · 신호위반 0 · 보행자 사고 0.**
  주행 검사기는 차로 중심을 `frame.lateral` 목표값으로 잡아야 한다 — 0 으로 잡으면 중앙선을 타고 달려 위반이 쏟아진다(처음 만들 때 그랬다).

### v0.9.18 — 인트로가 중반부터 하얗게 덮이던 것(흰 섬광 재발화) · 자막 판

소유자 지적: 「인트로 화면 중반부 넘어가면서 희끄므리 즉 넘 하얗게 표현이 되어서 정확한 전달이 안 되. 인트로를 제대로 점검하자」.

* **원인은 안개도 노출도 아니고, 흰 섬광(`#introFlash`)이 매 프레임 다시 터진 것이었다.**
  타이틀 등장(12.6초)을 `self.shot !== 'title'` 로 막고 `self.shot = 'title'` 을 세웠는데,
  같은 함수 **아래쪽에서 `if (SHOTS[i].ttl) self.shot = i;`** 로 샷 번호를 덮어쓴다 → 다음 프레임에 조건이 또 참.
  0.7초 애니메이션이 매 프레임 처음(불투명도 0.9)으로 되감겨 **12.6초부터 인트로 끝(26.4초)까지 흰 막이 화면을 덮었다.**
  같은 이유로 `t >= 14.3` 의 「타이틀 물러남」도 한 번도 실행되지 않아 타이틀이 뒤 샷을 계속 가렸다.
  → 타이틀·섬광 전용 플래그 `self.titleOn` / `self.titleOut` 로 분리했다. 덤으로 **13.5초에는 `.on` 을 코드로 걷어낸다** —
  탭이 숨거나 프레임이 밀려 CSS 애니메이션이 멈춘 환경에서도 흰 막이 남지 않는다(실제로 그 상태를 재현해 확인했다).
* **자막 뒤에 부드러운 어두운 판을 깔았다**: `.intro-shot::after`(왼쪽에서 오른쪽으로 사라지는 선형) ·
  `.intro-lines::before`(가운데 타원). 크레인 아웃처럼 화면이 밝은 장면에서도 글이 읽히고, 그림은 그대로 밝다.
  실측 배경 밝기: 자막 자리 t=19초 **150** · t=23초 **170** · t=25초 **172** (흰 섬광을 뺀 3D 자체 밝기) — 판 없이는 흰 글씨가 묻힌다.
* 계측 방법 기록: `readPixels` 로 캔버스 영역 평균 휘도를 재면 「하얗다」를 숫자로 잡을 수 있다.
  다만 **DOM 오버레이는 readPixels 에 잡히지 않는다** — 흰 막의 정체는 `getComputedStyle(...).opacity` 를 훑어서 찾았다
  (화면 절반을 넘게 덮는 요소만 걸러 나열 → `introFlash op=0.73` 이 나왔다).
* **브라우저 페인이 숨으면 rAF·CSS 애니메이션이 멈춘다** — 인트로를 실시간으로 볼 수 없다.
  `cine.jump(t)` + `cine.update()` + `T.render()` 로 시각을 찍어 보고, 전환은 `transition:none` 을 끼워 강제로 켜서 확인한다.
* 상시 검증에 **「인트로 흰 섬광」** 항목 추가: 인트로를 처음부터 0.1초 간격으로 굴려 `.on` **발화 1회** ·
  13.6초 뒤 불투명도 **최대 0** · 타이틀 물러남 **true** 를 확인한다. `T.intro()` 는 cine 객체를 새로 만들므로 참조를 다시 잡아야 한다.

### v0.9.19 — 실제 서초구로 맞추기(역 이름·랜드마크·향나무) · 도로 연결 실측

소유자 지시: 「실제 서초구의 도로 모양, 향나무 같은 랜드마크, 역 이름, 예술의전당 등을 잘 살려서
게임 속 화면만 보고도 『아 여기구나』 라고 맞출 수 있을 정도로 구현하고, 도로가 다 잘 연결되었는지 확인하고
끊어진 것이 보이면 연결하자」.

* **도로가 끊겨 보이던 진짜 이유를 찾아 고쳤다** — 포장이 아니라 **판정**이었다.
  `city.onRoadAny` 가 `terrain.nearest` 한 점만 보고 `dist <= half` 를 재는데, IC 합류부에서는 **폭이 좁은 램프**가
  본선보다 가까워 본선 바깥 차로(3·4차로)가 「도로 밖」으로 잡혔다. 그러면 그립이 0.72 로 떨어지고 HUD 에 도로 밖으로 뜬다.
  → `terrain.onDeck(x, z)`: `roadCands` 를 훑어 **어느 노면이든 하나라도 폭 안이면 도로 위**.
  실측 **모든 링크·격자 도로의 차로 중심 12,540 점 중 포장 끊긴 점 73 → 0**. IC 이음 16곳도 끝점 거리 ≤ 2m · 포장 끊김 0m.
* **교차로 이름을 실제 서초구 자리로** 옮겼다. 2호선 서초대로는 서 → 동으로 **방배역 · 서초역 · 교대역 · 강남역**이고,
  **서울성모병원·국립중앙도서관은 반포대로(사평대로~서초대로) 구간**, **고속터미널역은 신반포로**에 있다.
  전에는 성모병원이 서초역 자리에, 서초역이 교대역 자리에 있었다.
  - 하차 근무(교차로 근무) 무대도 `nodes[2][2]` → **`nodes[2][1]`(반포대로 × 사평대로)** 로 옮겼다 — 안내 문구 「서울성모병원 사거리」가 실제와 맞는다.
  - 새 이름: 동작대교 남단 · 이수역 · 잠원역 · 반포역 · 교대역 · 남부터미널역 · 한남대교 남단.
  - `signals.js` 의 교차로별 최소 녹색(NODE_MIN)도 새 이름 기준으로 다시 매겼다.
* **지하철역 출입구 13곳**(`city.subways` → `world.js`): 계단 옹벽·어두운 입구·난간·표지 기둥 +
  **노선 색 원(번호)과 역 이름 표지**(`TG.tex.subwaySign`). 실존 로고·상표는 쓰지 않고 번호와 이름만 적는다.
  고속터미널(3·7·9) · 잠원(3) · 반포(7) · 신논현(9·신) · 방배(2) · 서초(2) · 교대(2·3) · 강남(2·신) · 이수(7) ·
  남부터미널(3) · 양재(3·신) · 사당(2·4) · 내방(7 — 교차로가 아니라 서초대로 중간).
  실측 **차도 침범 0 · 보도 밖 0**. 표지는 **도로 쪽을 보게 180도 돌렸다**(그냥 두면 글씨가 거울로 보였다).
* **서초동 향나무**(서울특별시 기념물)를 서초역 옆에 세웠다 — 담장을 두른 마사토 마당(19m) 안에
  **줄기가 굵고 수관이 옆으로 넓게 퍼진 노거수** + 지주목 + 안내석(`TG.tex.stoneLabel`).
  마당과 겹치는 건물·나무는 걷어내고 담장을 충돌선으로 등록했다(실측 겹침 0).
* **새 랜드마크 두 곳**: `library` **국립중앙도서관**(반포대로 서쪽 · 석재 본관 + 가로 창띠 + 광장),
  `hospital` **서울성모병원**(반포대로 동쪽 · 흰 병동 42m + 옥상 헬리포트 + 응급실 캐노피 + 적색 띠).
  공원 블록은 **서리풀공원**(방배로~반포대로 · 효령로 남쪽)으로 옮겼다.
* **미니맵**에 역 13곳을 노선 색 점 + 이름으로 찍고, 성모병원 · 중앙도서관 · 예술의전당 · 법원·검찰 · 구청 ·
  고속터미널 · 서리풀공원 · 향나무를 제자리에 적었다.
* 상시 검증 2종 추가: **「서초구 알아보기」**(역 수 13 · 차도 침범 0 · 보도 밖 0 · 향나무 겹침 0 · 주요 역 존재) ·
  **「도로 연결」**(차로 중심 12,540 점 전부 포장 위). 교차로 이름도 서 → 동 순서로 찍어 확인한다.

### v0.9.20 — 한강을 도시와 올림픽대로 사이로 · 반포대교·한남대교가 실제로 강을 건넌다 · 세빛섬·한강공원·우면산·양재시민의숲

소유자 설계: 「올림픽대로에 연결되는 다리 2개를 놓자 — 반포대교와 한남대교. 이 두 다리를 건너면 다시 미니맵의
반대편으로 경부고속도로 순환되는 도로에 연결해서 계속 도로가 끝나지 않도록 하고, 도시가 확장되므로
세빛섬·한강시민공원, 그리고 다리를 건너면 연결되는 부분에 우면산·양재시민의숲을 넣으면 공간이 맞아떨어질 듯」.

* **다리라는 이름만 있고 강을 건너는 다리가 없었다.** 실측해 보니 `connN`(반포대교)·`connNE`·`connNW` 의 **다리점이 0** 이고,
  강은 z −212 에 있어 **링 북쪽 호(올림픽대로) 자체가 다리**였다. 즉 「반포대교」는 강에 닿지도 않았다.
  → **한강을 z −212 → −112 로 내렸다**(도시 z 0 과 올림픽대로 z 약 −245 사이).
  이제 **반포대교 29점 · 한남대교 12점 · 동작대교 14점**이 실제로 물 위를 건너고, 램프는 강 북쪽 마른 땅에서 링에 붙는다.
  `강남대로 · 신논현 방향` → **`강남대로 · 한남대교`(제한 80)**. 잠수교도 동작대교 아래에서 **반포대교 아래**로 되돌렸다.
* **순환이 끝나지 않는다**(실측 주행): 반포대로 북행 → **반포대교** → 진입로 → **올림픽대로** → **경부고속도로** →
  올림픽대로 → 경부고속도로 … 260초 주행에 **도로 밖 0초 · 순간이동 0 · NaN 0**. 강남대로 → 한남대교 → 램프 → 경부고속도로도 이어진다.
* **한강 시설**(전부 소품으로 만들었다 — `hBase` 봉우리를 넣으면 그 아래 도로가 함께 들려 파묻힌다, v0.9.2 교훈):
  - **세빛섬** 세 동(꽃봉오리 모양 원형 구조물 + 유리 띠 + 부유 기초) + 반포대교 쪽 보행 연결교.
  - **반포한강공원**: 강 남안 둔치에 잔디·산책로·**적색 자전거도로**·벤치·나무. 강 중심에서 **60m** 바깥에 깐다(46m 로 두면 물속에 잠겼다).
  - **다리 교각**: 물 위 상판 아래에 기둥 두 줄 + 가로보(반포대교·한남대교·동작대교·순환도로 강 구간).
* **서초구 남쪽**: **우면산**(초록 언덕 3단 + 경사면 숲 54그루 + 산자락 나무 26그루, 남부순환로 남쪽·경부고속도로 서쪽) ·
  **양재시민의숲**(잔디·산책로 격자·나무 54그루 + 매헌 기념관 형태의 전시동, 경부고속도로 동쪽).
  자리는 **모든 링크에서 최소 여유 거리를 실측해** 골랐다(우면산 81m · 양재시민의숲 70m).
* **함정 두 개 기록**: ① `GeoBuilder.cylinder` 의 반경은 **(아래, 위) 순서** — 뒤집어 넣으면 산이 사발처럼 위가 벌어진다.
  ② 경사면에 나무를 세울 때는 **원뿔 식으로 그 지점 높이를 구해야** 한다 — 대충 놓으면 나무가 산 속에 파묻힌다.
* 미니맵에 반포대교·한남대교와 `terrain.scenery`(세빛섬·반포한강공원·우면산·양재시민의숲) 이름을 찍는다. 한강 선도 `terrain.riverZ` 를 공유한다.
* 검증: verify **ERRORS(0)** · 도로 연결 **12,545점 / 끊김 0** · IC **8/8 STUCK 0** · 3분 소크 NaN 0 · 잠수교·철길·침수 검사 통과.

### v0.9.21 — 교통 심시티 첫 층: 교차로 신호값 설정 · 무인 교통단속 장비

소유자 방향: 「심시티 하듯이 도로와 사거리 신호등 위치·신호값 등도 만들고 설정할 수 있어야 한다.
심시티적 요소도 넣고 교통기능 시설 개선, 신호·과속 카메라 설치 같은 것도 가능하게 한다.
즉 교통 심시티 및 니드포스피드 · GTA 단속을 구현하는 거다. 그래야 확장성이 생길 듯」.

* **신호기가 교차로마다 다른 주기를 갖게 됐다**(엔진 변경). 전에는 `SIG_GREEN` 하나로 모든 교차로가 같은 30초 녹색이었다.
  이제 `ctrl[key].gv`(남북)·`gh`(동서) 를 따로 두고 `phase(t, gv, gh)` 가 한 주기 = `gv + Y + R + gh + Y + R` 로 돈다.
  `state`·`pedWalk`·`pedRemain`·`greenNow`·`waitFor`·`set`·수동 유지 로직을 전부 노드 인식으로 바꿨다.
  - **보행 시간은 여전히 줄일 수 없다**: `greenMin(node, axis)` = max(그 교차로 최소 녹색, 직각 횡단보도 보행 시간 + 2초).
    반포대로(8차로)를 건너야 하는 동서 녹색은 하한이 **40초**, 서초대로를 건너는 남북은 **34초** — 도로 폭에서 나온 값이다.
  - 실측: 기본 주기 69초 → 남북 46초로 올리고 동서를 4초로 내리려 하면 **하한 40초에서 멈춘다**. 새 주기 95초.
    남북 녹색이 실제로 **45초** 유지되는 것까지 확인했다.
* **`js/facil.js` 교통시설 관리**(심시티 요소):
  - **교차로 신호 녹색 시간**을 ±2초씩 조절. 하한에 걸리면 「보행 시간이 먼저입니다」로 알린다.
  - **무인 교통단속 장비 설치**: 접근로마다 🚦 신호위반 / 📷 과속. 문형 지주 + 팔 + 함체 두 대 + **예고 표지**(`TG.tex.camSign`)가 선다.
  - 단속은 실제로 작동한다 — 적색에 정지선을 넘으면 신호위반, 제한속도 + 11km/h 초과면 과속.
    **순찰차가 걸리면 감점**(경찰이 먼저 지킨다), 일반 차량이 걸리면 +10점과 「📷 무인 단속」 안내.
    제한속도는 도로에서 직접 읽으므로 **어린이보호구역에서는 30km/h 기준**이 된다.
  - 설정은 `localStorage tg_facil` 한 곳에 저장된다(`tb_` 키는 읽지도 쓰지도 않는다).
* **화면**: 타이틀에 **「🏗 교통시설」**. 왼쪽에 교차로 25곳 목록(주기·녹색·설치 대수), 오른쪽에 설정판.
  3D 화면을 찍어 고르는 방식이 아니라 **목록에서 골라 큰 단추로 바꾸는 방식** — 폰에서 손가락으로 쓸 수 있다.
* **점수 NaN 방어**: `addScore` 에 `isFinite` 가드를 넣었다. 없는 감점 키(`penalize('signal', ...)`)를 주자 점수가 NaN 이 되어
  HUD·결과 카드가 통째로 깨졌다 — 감점 키는 `config.SCORE` 에 있는 이름(`redLight`·`speeding` …)이어야 한다.
* **일곱 번째 주석 사고**: `applyMode` 를 패치할 때 한 줄 주석 뒤에 원래 문장이 이어 붙어 **함수 본문 전체가 주석 처리**됐다
  (`main.js` 파싱 실패 → TG.test 없음). `new Function(src)` 로 파일별 문법을 먼저 확인하면 바로 잡힌다.
  **주석은 줄 끝이거나 다음 줄. 패치 문자열이 주석으로 끝나면 반드시 개행을 붙인다.**
* 상시 검증 3종 추가: 신호값 설정(기본주기·올림·하한걸림·새주기) · 남북 녹색 유지 시간 · 무인 단속(설치·적발·감점·점수 유한) · 교통시설 화면.

### v0.9.22 — 지도 레이어(디지털 트윈 첫 층): TAAS 사고 데이터 자리 · 시뮬레이션 위험도

소유자 방향: 「TAAS 정보 즉 교통사고 데이터를 입혀서 디지털 트윈이 구현되는지도 시도해 보자.
맵을 레이어로 구성해서 데이터들을 쌓으면서 보면 게임이라지만 시뮬레이션 세상이 된다.
처음부터 완벽하지는 않겠지만, 군대에서 하는 워 게임 같은 것을 실제 도로 데이터들을 받아서 돌리는 것이다. 차분하게 진행하자」.

* **첫 층은 「레이어 엔진 + 데이터가 들어올 자리」다.** 사고 건수를 **코드에 적지 않았고, 지어내지도 않았다** —
  이 프로젝트 규칙(1차 출처에서 확인한 것만)을 그대로 지켰다. 그리고 **게임은 네트워크 요청 0** 이므로
  TAAS OpenAPI 를 실시간으로 부를 수 없다. 그래서 **`data/taas.json` 파일 한 곳**에서만 읽는다(`laws.json` 과 같은 방식).
* `data/taas.json` — 스키마 `tg-taas/1`. 레이어 3개(`taasHot` 사고다발지 · `taasPed` 보행자 · `taasChild` 어린이).
  **items 는 비어 있다.** `taasHot` 에만 `example:true` 견본 한 건이 있고 화면에 **「예시」** 로 표시되며 통계에 넣지 않는다.
  `howto` 에 넣는 방법을 적어 두었다 — 지점은 `node:[i,j]`(i 서→동, j 북→남) 또는 `xz:[x,z]`(교차로 간격 80m).
  **소유자가 TAAS 자료를 이 파일에 넣으면 그날 바로 지도에 뜬다.**
* `js/layers.js` — 레이어 엔진. 세 갈래로 나눴다.
  - **외부 데이터**: TAAS 사고다발지(원의 크기 = 건수). 파일에 있는 값만 쓴다.
  - **지도가 이미 아는 것**: 어린이보호구역(노란 면) · 무인 단속 장비(초록 점).
  - **시뮬레이션이 만드는 것**: **위험도** — 급제동·보행자 근접·신호위반·무인 단속 적발을 **교차로별로 쌓는다**.
    외부 자료가 없어도 오늘 달리면 바로 쌓이고, 나중에 TAAS 레이어와 **같은 지도 위에서 비교**할 수 있다. 이게 워게임의 씨앗이다.
  - 3D 노면에 반투명 원·면으로 겹치고, **미니맵에도 같은 자리에 겹쳐** 그린다. 켜고 끈 상태는 `localStorage tg_layers`.
  - 사고다발지 안에 들어가면 「⚠ (예시) 사고다발지 — … · 감속」 안내가 뜬다(레이어를 켠 경우에만, 14초 간격).
* 화면: **🏗 교통시설** 아래에 **「🗺 지도 레이어」** 단이 붙었다. 레이어별 켜짐/꺼짐 · 항목 수 ·
  **데이터가 없으면 「데이터 없음 — TAAS 자료를 넣으면 지도에 뜹니다」**, 견본만 있으면 「예시만 있습니다」로 솔직히 말한다.
  아래에 출처·연도·넣는 방법과 「위험도 초기화」.
* **함정 기록**: 부채꼴(원) 삼각형은 위에서 볼 때 감김 방향이 뒤를 보는 경우가 있어 거의 안 보인다 → 레이어 재질은 **양면**(`DoubleSide`).
* 상시 검증 2종: 레이어 목록·출처·항목 수·**실데이터 0건**(견본은 세지 않는다) / 위험도 누적 · 켠 상태에서만 사고다발지 판정.
* 검증: verify **ERRORS(0)** · 도로 연결 12,545/0 · 신호값·무인 단속·교통시설 화면 통과 · 3분 소크 NaN 0.

**다음 층(합의 필요)**: ① TAAS 실제 자료 입력(소유자가 파일을 주면 바로) ② 교차로별 지체시간·대기행렬 지표를
시뮬레이션에서 재서 「개선 전/후」 비교 ③ 도로 자체 편집(차로 수·회전교차로·좌회전 차로) ④ 안전시설(과속방지턱·보행섬).

### v0.9.23 — TAAS 실제 교통사고 데이터를 지도에 얹었다(서초구 23건) · 오픈API 경로 확인

소유자 지시: 「클로드인 크롬으로 들어가서 데이터를 받아와라 … 그리고 이후 사용자들은 자신들이 보고 싶어 하는
지역의 맵을 다운받아서 할 수 있도록 오픈 API 가 사용 가능한지 알아보고」.

* **Claude in Chrome 확장은 연결돼 있지 않았다**(`list_connected_browsers` → 빈 목록). TAAS 는 로그인이 필요 없는 공개 통계라
  앱 안 브라우저로 받았다. 결과는 같다.
* **받은 곳**: 도로교통공단 **교통사고정보 개방시스템**(KOROAD OPEN API PORTAL, `https://opendata.koroad.or.kr/api/`).
  TAAS 메인 → 「교통사고정보 개방체계」 → `web.shp.adi.gotoOpenApi()` 가 이 주소를 연다.
* **오픈 API 는 있다 — 16종.** 다발지역 12종(자전거·보행노인·보행어린이·어린이보호구역내 어린이·법규위반별·연휴기간별·
  지자체별·결빙·이륜차·보행자·음주운전·화물차) + 교통안전정보 3종(링크기반 위험지역·세부링크 도로위험지수·고속도로구간별 위험지수)
  + 통계 1종(지자체별 대상 교통사고 통계). 형식 JSON/XML/CSV, REST(+일부 WMS).
  - 요청: `https://opendata.koroad.or.kr/data/rest/frequentzone/pedstrians?authKey=…&searchYearCd=2023&siDo=11&guGun=680&type=json`
    (`siDo`·`guGun` = 법정동 코드. **서울 11 · 서초구 650**. 코드 목록도 내려받을 수 있다.)
  - 출력: `spot_nm`(지점명) · `occrrnc_cnt`(사고건수) · `caslt_cnt`(사상자) · `dth_dnv_cnt`(사망) · `se_dnv_cnt`(중상) ·
    `sl_dnv_cnt`(경상) · `lo_crd`/`la_crd`(경도·위도 EPSG:4326) · `geom_json`(폴리곤).
  - **인증키가 필요하지만(회원가입), CSV 다운로드는 키 없이 열려 있다** — 「openAPI 를 활용하지 못하는 분들이
    데이터를 활용하실 수 있도록 CSV 로 제공합니다」. 이 게임은 **네트워크 요청 0** 이므로 실시간 호출은 하지 않는다.
    사용자는 자기 지역 CSV 를 받아 `data/taas.json` 에 옮기면 그 지역 지도가 된다 — 그 방법을 파일 `howto` 에 적어 두었다.
  - **출처 고지 의무**: 「TAAS 에서 제공된 자료를 활용 시 출처 및 자료링크를 명시해 주시기 바랍니다」 →
    `attribution` 필드를 만들고 레이어 패널에 **항상** 보이게 했다.
* **넣은 실제 데이터**(`data/taas.json`, 스키마 `tg-taas/2`): 서초구 **23건**, 2025·2026년 공표분.
  - 전체 다발지 6건(`17_25_lg.csv`) — 양재동 트럭터미널앞교차로 50건 · 잠원동 반포사거리 48건 · 반포동 오딘약국 46건 ·
    **반포동 서울성모병원교차로 44건**(하차 근무 무대다) · 잠원동 고속터미널교차로 46건.
  - 보행자 8건(`19_25_pedstrians.csv`) — 서초동 굿약국 13건(중상 13) · 양재동 엔스온누리약국 8건(**사망 1**) ·
    반포동 고속터미널역 8건 · 방배동 사당역사거리 7건 등.
  - 보행어린이 2건 · 이륜차 6건(서초동 서울교대교차로 포함) · 자전거 1건.
* **좌표 변환**: 실제 위경도 → 축약 지도. `u=lon-127.01, w=lat-37.49` 일 때
  `x = 6056.10u + 1401.41w + 183.17`, `z = 1426.88u − 11757.20w + 210.60`.
  사당역·고속터미널역·강남역·양재역 **네 지점 최소제곱 맞춤**, 그 네 곳 오차 5~26m.
  **지점명이 게임 교차로를 그대로 가리키는 항목**(사당역사거리·고속터미널역·반포사거리·서울교대교차로)은 좌표 대신
  `node:[i,j]` 로 붙였다 — 좌표 추측이 아니라 지점명이 근거다. 나머지는 `approx:true` 로 「위치 근사」라고 화면에 밝힌다.
* 원 크기는 건수만이 아니라 **사망 ×6 · 중상 ×0.6 가중**으로 정한다(전체 다발지는 50건대라 건수만 쓰면 화면을 덮는다).
  최대 반경 50m — TAAS 다발지 기준이 반경 100m 라 그 안에 둔다.
* 사고다발지에 들어가면 「⚠ 보행자 사고다발지 — 서초동(굿약국 부근) (2026년 공표) · 13건 · 중상 13 · 위치 근사 · 감속」.
* 검증: verify **ERRORS(0)** · 레이어 8종 · 보행자 8건 전부 `verified` 및 좌표·연도 있음 · 켠 상태에서만 판정 ·
  도로 연결 12,545/0 · 신호값·무인 단속·교통시설 화면 통과 · 3분 소크 NaN 0.

### v0.9.24 — 지도를 파일로 뺐다(지도 선택 1단계) · 첫 지도 「서울 서초구(베타)」

소유자 질문: 「그럼 지도 선택으로 이 모든 걸 구현할 수 있는 거지? 최초 맵은 베타 버전이고」.

* **답: 세 단계로 나뉜다. 이번 판은 1단계다.**
  - **1단계(지금 됨)**: 이름·랜드마크·지하철역·어린이보호구역/공원 블록·TAAS 사고 자료를 **지도 파일**에서 읽는다.
    격자 모양은 그대로이고, 그 위에 얹히는 「이 도시가 어디인가」가 통째로 바뀐다.
  - **2단계(다음)**: 격자 자체를 지도 파일로 — 도로 개수·간격·차로 수·일방통행. 5×5 고정이 아니라 그 구의 간선 배치에 맞춘다.
  - **3단계(진짜 디지털 트윈)**: 표준노드링크·OSM 도로망을 읽어 링크·노드를 자동 생성. 오프라인 변환 도구가 필요하다.
* `data/maps/index.json`(목록) + `data/maps/seocho.json`(첫 지도). `?map=<id>` · 저장된 설정 · 목록 기본값 순으로 고른다.
  `city.js` 는 `TG.MAP` 이 있으면 그 값을, 없으면 코드 안 기본값(서초구)을 쓴다 — **파일을 못 읽어도 게임은 돈다**(file:// 포함).
* 첫 지도는 **베타**로 표시한다. 타이틀 판 번호 옆에 「v0.9.24 · 지도: 서울 서초구 (베타)」,
  교통시설 화면 맨 위에 **🗺 지도** 단(목록·베타 배지·새 지역 넣는 방법).
* 지하철역에 `midV:[i,i+1]` 을 넣을 수 있게 했다 — 내방역처럼 **교차로가 아니라 도로 중간**에 있는 역을 지도 파일로 표현한다.
  향나무 같은 점 랜드마크는 `atBlock:[i,j]` 로 블록 모퉁이에 놓는다(좌표를 직접 적지 않아도 된다).
* **여덟 번째 함정 — 이번엔 주석이 아니라 이름 충돌이었다.** 부팅용 함수를 `start()` 로 만들었더니
  같은 스코프에 이미 있던 **게임 시작 함수 `start(carId, mode)` 를 덮었다**(함수 선언은 뒤가 이긴다).
  그 결과 `T.start('sedan')` 이 부팅 함수를 불러 **단속·조작이 통째로 죽었다**(brake-hold 가 D/29, 조향 0, `G.enforcement` 없음).
  `bootInit()` 로 이름을 바꿔 고쳤다. **같은 파일에 흔한 이름의 함수를 새로 만들 때는 먼저 `grep -n "function <이름>"` 한다.**
* 검증 방식도 고쳤다: 지도 파일을 먼저 읽고 init 이 돌기 때문에 **고정 2.5초 대기는 위험하다** →
  `_verify/_ic/_probe` 꼬리가 `TG.test.game.city·signals·hud` 가 다 생길 때까지 기다린 뒤 시작한다.
* **인증키는 이 저장소에 들어오지 않는다.** 소유자 지식베이스(`07_API키/`)에 공공데이터포털·서울 열린데이터광장·
  경기데이터드림 키가 있지만, 이 저장소는 **공개 + GitHub Pages** 이고 게임은 **네트워크 요청 0** 이라 키가 있을 자리가 없다.
  자료를 더 받아야 하면 키는 **로컬에서만** 쓰고 **결과만** `data/taas.json` 에 정적으로 굽는다. 실제로 v0.9.23 데이터는
  키 없이 공개 CSV 로 받았다. 저장소에 `authKey`·`apiKey` 흔적이 없는지 판올림 때 grep 으로 확인한다.
* 검증: verify **ERRORS(0)** · 지도(이름·베타·파일에서 읽음·역 13) · 지도 선택 화면 · 레이어 8종 ·
  신호값·무인 단속·교통시설 화면 · 도로 연결 12,545/0 · IC 8/8 STUCK 0 · 3분 소크 NaN 0 · 저장소에 인증키 흔적 0.

## v0.9.25 — TAAS GIS 교차로별 사고 집계(디지털 트윈 두 번째 층)
* **자료 출처가 바뀌었다.** v0.9.23 은 TAAS **다발지 공표자료**(23건, 지점 단위)였다. 이번은 TAAS **GIS 사고분석**
  (`POST /gis/srh/ash/selectAccidentInfo.do`, 서울 서초구 11650, 2023~2025, `acdntGaeCode` 4갈래 전부)에서
  **개별 사고 6,343건**을 받아 **교차로 25곳에 집계**했다. 사망 30 · 중상 1,156 · 경상 4,968 · 부상신고 189.
  교차로 반경 40게임단위(약 585m) 안 **3,257건 배정 · 3,086건 바깥**. 바깥은 격자에 없는 이면도로·고속도로 본선이다.
* **개인정보는 애초에 담지 않았다.** 응답에는 `acdnt_age_1_dc`·`sexdstn_div_1_dc`·`bdy_injury_part_*`·`acdnt_no` 가
  들어 있지만 **수집 단계에서 버렸다** — 「개인정보가 든 파일은 올리지 않는다」. 파일에 남은 것은 교차로별 **건수·등급·
  사고유형·법규위반·차종·도로형태**뿐이다. `notice` 에 이 사실을 적어 두었고, verify 가 항목 열이름을
  `age/sex/injury/bdy/acdnt_no/birth` 로 훑어 **0개**임을 확인한다.
* `data/taas-nodes-seocho.json`(schema `tg-taas-nodes/1`) 신설 → 지도 항목 `taasNodes` 로 연결.
  레이어 `taasNode`(교차로별 사고 집계) 한 종이 늘어 **8종**이 됐다. 원 크기는 건수가 수백이라
  **제곱근**으로 눌렀다(반경 12~38) — 선형으로 두면 원이 화면을 덮는다.
* 순찰 중 그 교차로에 들어가면 HUD 가 **사고경위(법규위반) 상위 2개**까지 붙여 준다.
  「⚠ 교차로별 사고 집계 — 반포역 사거리 (2023~2025) · 297건 … · 안전운전불이행 121 · 안전거리미확보 63 · 감속」
* 상위: 반포역 297(사망2) · 고속터미널 289 · 교대역 245(사망1) · 양재역 239(사망3) · 잠원역 227 · 서울성모병원 222 · 서초역 178.
* 검증: verify **ERRORS(0)** · 교차로 집계(25곳 · 합계 3,257 = 배정건수 · 좌표·건수 이상 0 · 경위 없음 0 · **개인속성 0**) ·
  출처·연도·수집/배정/바깥·등급·안내 모두 있음 · 최다지점 HUD 판정 O, 반경 밖 X · 레이어 8종 ·
  도로 연결 12,545/0 · IC 8/8 reached · 3분 소크 NaN 0 · 저장소에 인증키 흔적 0.

## v0.9.26 — 사망사고 55건(2020~2025) 사례 레이어 + 인트로에 자료 출처
* **소유자 방향이 바뀌었다.** 「자잘한 사고는 데이터로 가지고 있지만 **사망사고를 다루는게 일단 중요**해.」
  그래서 사망사고는 **뭉치지 않는다** — 서초구 2020~2025 사망사고 **55건(사망 58명)**을 한 건씩 사례로 둔다.
  자잘한 사고의 교차로별 집계(v0.9.25)와 **다른 파일·다른 레이어**다.
* `data/taas-fatal-seocho.json`(schema `tg-taas-fatal/1`) 신설. 레이어 `taasFatal`(사망사고) → 레이어 **10종**.
  이 지도 안 49건 · 바깥 6건(양재·내곡 방향은 축약 격자에 없다).
  연도별 2020년 9 · 2021년 8 · 2022년 8 · 2023년 7 · 2024년 **15** · 2025년 8.
  법규위반 안전운전불이행 29 · 기타 13 · 안전거리미확보 4 · 중앙선침범 4 · 신호위반 3 · 보행자보호의무위반 2.
  **가해 차종** 승용 28 · 승합 6 · 화물 6 · 이륜 5 · 건설기계 3 · 자전거 3 · PM 2. **피해** 보행자 **28**(절반이 넘는다).
* **원 크기로 세기를 나타내지 않는다** — 한 건은 한 건이라 반경을 11로 고정했다.
  그 자리에 들어가면 HUD 가 건수 대신 **그날의 조건**을 읽어 준다:
  「🕯 사망사고 — 2024년 1월 · 차량단독 · 기타 · 야간 05시 · 토요일 · 맑음 · 기타/기타 · 중앙선침범 · 승용」
* **TAAS GIS 는 한 번에 3년까지만 조회된다**(끝 연도 목록이 시작 연도+2 까지만 열린다).
  **결과 건수에는 한도가 없다** — v0.9.27 에서 699건이 한 번에 오는 것을 확인했다.
  ~~3년치가 25건에서 잘린다~~ 고 적었던 것은 틀렸다. 그 25건은 실제 총계였다(9+8+8).
  `totalPageCount` 는 25건 기준으로 계산돼 나오지만 목록은 이미 전량이고,
  `pageIndex`·`pageUnit`·`currentPageNo` 는 아무 효과가 없다. 연도별로 나눠 부르는 것은 확인이 쉬워서다.
  파라미터 이름은 추측하지 않고 페이지의 XHR 을 후킹해 확인했다
  (`selectAccidentInfo.do` · JSON 본문 `searchType/zoneYn/engnCode/startAcdntYear/endAcdntYear/legaldongCode/acdntGaeCode`,
  `acdntGaeCode` 01 사망 · 02 중상 · 03 경상 · 04 부상신고, `legaldongCode` 는 `11650%`).
* **개인정보는 이번에도 안 받았다.** 나이·성별·상해정도·상해부위·사고번호 칸이 응답에 있지만 수집 단계에서 버렸다.
  **날짜도 「연·월」까지만** 담고 일자는 버렸다 — 사망사고는 일자와 장소가 겹치면 특정 사건을 지목할 수 있다.
  verify 가 항목 열이름을 `age/sex/injury/bdy/acdnt_no/birth/dd_dc` 로 훑어 **0개**임을 확인한다.
* **인트로에 자료 출처를 넣었다**(소유자: 「데이터 출처를 꼭 넣어달라고 했으니까」).
  21초부터 끝까지는 자막이 없던 구간이라 그 자리에 `#introCredit` 를 둔다 —
  「실제 교통사고 자료를 얹었습니다 / 자료 · 도로교통공단 교통사고분석시스템(TAAS) / 서울 서초구 사고다발지 ·
  교차로별 집계 · 사망사고 2020~2025 / 축약 지도 위의 자리라 실제 사고 지점과 다릅니다 · 개인정보는 담지 않았습니다」.
  `jump()` 으로 시간을 되감을 수 있으므로 **켜는 쪽과 끄는 쪽을 둘 다** 뒀다(검증이 되감는다).
* 미니맵 채움을 `aa`(67%) → `55`(33%) 로 묽혔다. 두 층을 같이 켜면 점이 74개가 되어 **도로 이름이 안 보였다**.
* 검증: verify **ERRORS(0)** · 사망사고(지도 안 49 = inMap · 사례문 없음 0 · 원 크기 제각각 0 · **개인속성 0** ·
  수집 55 = 안 49 + 바깥 6 · 사망 58 · 연도 6개 · HUD 사례 판정 O) · 인트로 출처(21초 전 꺼짐 → 뒤 켜짐 · TAAS·개인정보 표기 O) ·
  레이어 10종 · 도로 연결 12,545/0 · IC 8/8 reached · 3분 소크 NaN 0 · 저장소에 인증키 흔적 0 ·
  브라우저 실사 확인(인트로 22초 화면 · 교통시설 패널의 사망사고 단 · 순찰 중 HUD).

## v0.9.27 — 어린이·보행자·노인·자전거 6개 레이어(홍보용 분포 포함)
* 소유자: 「안타까운 어린이사고나 보행자사고들도 표현을 해주면 좋겠어 특히 **교통안전홍보 활동시 데이터도 필요**하거든」
* `data/taas-vuln-seocho.json`(schema `tg-taas-vuln/1`) — TAAS GIS **간편조건**(`searchSimpleCondition`)으로 6갈래를
  2020~2025 전량 받았다. 어린이 395 · 어린이 보행자 90 · 어린이보호구역 109 · 보행자 **2,236** · 노인 보행자 336 · 자전거 293.
  레이어 `vuln_*` 6종 추가 → 전체 **16종**.
* **집계와 사례를 갈래마다 다르게 뒀다.** 건수가 많은 것(보행자·노인·자전거·어린이·보호구역)은 **교차로 부근 집계**로,
  건수가 적은 **어린이 보행자(90건)**만 한 건씩 사례로 둔다(지도 안 49건). 교차로 이름은 파일이 아니라 **지도**가 가진다
  (`city.nodeName`) — 지도를 바꾸면 이름도 따라 바뀐다.
* **홍보에 그대로 쓸 문장 9개**를 파일의 `highlights` 에 넣었다. 전부 같은 파일의 숫자에서만 뽑았다:
  어린이 사고 395건에 **사망 0** · 어린이 사고는 **14~18시에 181건(46%)** · 어린이 보행자 사고 가해 법규위반은
  안전운전불이행 43 + 보행자보호의무위반 27 로 78% · 어린이보호구역 사고는 2020년 15 → 2025년 26 으로 늘었고
  교차로(68) > 단일로(28) · 보행자 사고 가해차종에 **이륜 117 · 자전거 98 · PM 72** — 위험한 것이 자동차만은 아니다 ·
  노인 보행자 사고는 중상사고 비율 **47%**(어린이 18%) · 자전거 사고는 5월(46)·10월(37)에 몰리고 **보도통행중 37건**.
* **개인정보는 이번에도 안 받았다.** 「어린이」·「노인」은 파일에 담긴 값이 아니라 **조회 조건**이다.
  사례에도 날짜는 「연·월」까지만 담았다. verify 가 열이름을 훑어 개인속성 **0** 을 확인한다.
* **앞 판의 잘못된 기록을 고쳤다** — TAAS GIS 결과에 건수 한도는 없다(699건이 한 번에 왔다). 3년 조회가 25건이던 것은
  그것이 실제 총계였기 때문이다. 조회 범위 한도(3년)만 실재한다.
* 검증: verify **ERRORS(0)** · 어린이·보행자(레이어 6 · 항목 이상 0 · 이름 없음 0 · **집계 합 = 전체−바깥** 일치 ·
  개인속성 0 · 홍보 문장 9 · 어린이 사망 0 · 14~18시 181 · 보행 사례 49) · 레이어 16종 ·
  도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.28 — 12대 중과실 조문 원문 대조 정정 · 사거리 신호등 6곳 누락 · 보행 신호 시간 · 학습 화면 탈출
소유자 신고 네 건을 한 판에 고쳤다. 「조금이라도 틀리면 안 되는 부분」이라 조문은 원문으로 대조했다.

### ① 12대 중과실 조문 — **틀린 것이 있었다**
국가법령정보센터 공동활용(로컬에서만 조회, 키는 저장소에 없다)으로 **교통사고처리 특례법**(시행 2025.6.4.)과
**도로교통법**(시행 2026.7.1.) 원문을 받아 12개 항목을 한 줄씩 대조했다. 고친 것:
* **제3호(제한속도 20km/h 초과) — 「제17조 제3항」은 틀렸다.** 단서 제3호가 인용하는 것은 **제17조 제1항 또는 제2항**
  (제한속도의 근거)이다. 제3항은 「최고속도보다 빠르게 운전하여서는 아니 된다」는 준수 규정. 둘 다 적었다.
* 제2호에 **제62조**(고속도로 횡단·유턴·후진 금지)가 빠져 있었다 — 단서 제2호는 제13조제3항 **또는 제62조**다.
* 제4호는 「제21조부터 제23조까지」가 아니라 **제21조 제1항 · 제22조 · 제23조**이고, **제60조 제2항**(고속도로 앞지르기 방법)이 빠졌다.
* 제7호에 **건설기계관리법 제26조**와 **도로교통법 제96조**(국제운전면허증)가 빠졌다. 제152조는 단서가 인용하는 조문이 아니라 **벌칙**이라 그렇게 적었다(제152조 **제1호** — 항이 하나뿐이라 「제1항 제1호」는 틀린 표기였다).
* 제8호에 **제45조**(약물)가 빠졌다. 제148조의2 는 벌칙.
* 제11호는 **제12조 제1항·제3항**이고 「어린이를 **상해**에 이르게 한 경우」다. 특정범죄가중처벌법 제5조의13 은
  단서 제11호가 아니라 **별개의 가중처벌** 조문이라 그렇게 구분해 적었다.
* 제1호는 특례법이 「제5조」를 통째로 인용하므로 항 표시를 뺐다. 제5·6·9·10·12호는 원문과 일치했다.
`data/laws.json` 의 `study12.source` 에 **무엇을 어떻게 고쳤는지** 남겼다.

### ② 사거리인데 신호등이 3개 — 6곳
`main.js` 가 **world 를 terrain 보다 먼저** 만들고 있었다. `city.exitFor` 는 terrain 이 붙기 전엔 늘 null 이라,
연결로에서 들어오는 접근로에 차량 신호등이 안 세워졌다 — **강남역·고속터미널·이수역·예술의전당** 네 곳.
`hasStub` 을 조건에서 빠뜨려 **잠원역·방배로 신반포로** 두 곳도 빠졌다.
`world.js` 는 terrain 을 전혀 쓰지 않으므로 **terrain → attachTerrain → world** 순서로 바꾸고, 조건에 `hasStub` 을 넣었다.
verify 가 25개 교차로의 **접근로 합 = 차량등 합**(90 = 90)을 확인한다.

### ③ 보행 신호가 짧아 다 건너기 전에 적색
`pedTime` 이 「**차량 녹색 − 2초**」로 값을 깎고 있었다. 기본 녹색 30초가 넓은 횡단보도(37m)의 보행 시간(37.2초)을
못 품어서, 보고 출발해도 중간에 적색이 됐다. **보행 시간은 깎지 않는다** — 대신 각 교차로 녹색 초기값을
`greenMin` 까지 끌어올린다(`raiseToPedMin`). 이제 보행 25~37.2초, 「차량 녹색 ≥ 보행 + 2」가 25곳 전부에서 성립한다.

### ④ 학습 화면에 갇힘
닫기 단추가 긴 카드 **맨 아래**에만 있었다. 위에 **따라오는(sticky) 닫기 줄**을 붙이고, **Esc** 와 **바깥 클릭**으로도 닫히게 했다.
Esc 는 학습 화면이 열려 있으면 일시정지보다 먼저 그것을 닫는다.

* 검증: verify **ERRORS(0)** · 신호등 90/90 불일치 0 · 보행 신호 짧은 곳 0 · 학습 화면 Esc 로 닫힘 ·
  도로 연결 12,545/0 · 3분 소크 NaN 0 · 레이어 16종 유지.

## v0.9.29 — 신뢰도 판: 겹침 · 앞뒤 보행등 · 횡단자용 4색등 · 손들기 유지
물리와 신호가 어긋나면 그 뒤에 얹은 어떤 자료도 믿기지 않는다. 그래서 이 판은 눈에 보이는 신뢰도만 고쳤다.

* **사람이 사람을, 사람이 차를 통과했다**(소유자 신고). `peds.js` 에 겹침 풀기를 넣었다 —
  어깨 반경 0.34m(둘이면 0.68m)로 서로 밀어내고, 차체 사각형(길이·폭 + 반경) 안에 있으면 **가장 가까운 변**으로 밀어낸다.
  걸음을 다 옮긴 **뒤에** 풀고 그 자리를 그린다.
  * **조작하는 사람은 밀지 않는다** — 밀면 횡단보도에서 차도로 튕겨 나간다. 상대만 옮긴다.
  * **함정**: `peds.player` 는 보행 모드에서 **사람**이다. 그것을 차 목록에 넣었더니 자기 자신을 차로 보고
    매 프레임 자기를 밀어, 교차로 근무 경찰관이 **1초에 37m** 미끄러져 벌판으로 나갔다(소유자: 「벌판에 서있다」).
    `self.player !== self.walker && self.player.wid && self.player.len` 로 걸렀다. 검증에 「2초 이동 < 1m」을 넣었다.
* **횡단보도 앞·뒤 양쪽에 보행등이 보여야 한다**(소유자 신고). 보행판을 **뒤를 보는 판**과 짝지어 세웠다(180 → 360장).
  또 판이 함체 앞 **0.01m** 에 있어 멀어지면 묻혔다 — **0.09m** 로 빼고 함체를 얕게 했다.
* **횡단하는 사람 눈에 4색 신호가 하나도 안 보였다**(소유자: 「어린이 홍보물에도 전면 4색 신호등이 보이지 않아」).
  차량등이 모두 운전자 쪽만 향했기 때문이다. 같은 기둥 4.30m 높이에 **길 건너편을 보는 보조 4색등**을 달았다(교차로당 4개, 90개).
  이것이 보여 주는 것은 **건너는 도로의 차량 신호**다 — 보행 녹색이면 이 등은 적색이다.
  검증이 81회 표본에서 **어긋남 0** 을 확인한다(보행 녹색인데 그 도로 차량이 녹색인 경우 0).
  * 보행↔차량 짝 자체는 원래 맞았다(72회 검사 어긋남 0). 문제는 **보이는 자리에 면이 없었다**는 것이다.
* **어린이가 손을 바로 내렸다**(소유자 신고). `raiseHand(4)` 의 4초가 지나면 **초록불을 기다리는 동안** 팔이 내려갔다.
  대기 중에도 `walker.hand` 를 1.0 이상으로 유지하고, 횡단보도 위에서는 1.5 로 유지한다. 횡단보도를 벗어나면 해제.
* 검증: verify **ERRORS(0)** · 겹침(겹친 둘 → 0.68m, 차체 밖으로) · 교차로 근무(2초 이동 0m · 제어함 2.12m) ·
  보행판 360/뒤판 180/보조등 90/짝 어긋남 0 · 신호등 90/90 · 보행 신호 25~37.2초 짧은 곳 0 ·
  어린이 횡단 별 3개 정상 · 도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.30 — 음주운전 적발 절차(원문 대조 후 구현)
소유자: 「음주운전 감지기와 측정기가 있는거 알지 **적발절차를 미리 다 찾아 놓고** 게임이지만 현실을 반영해서 하자」
「게임이라고 해도 법과 원칙을 모두 지켜야 하니 **구현된것과 현실의 괴리가 없어야 함**」

### 먼저 원문을 다 찾았다(국가법령정보 공동활용, 2026-09-10)
* **도로교통법 제44조** — 제2항 호흡조사(운전자는 응하여야 한다) · 제3항 불복 시 **운전자의 동의를 받아** 혈액 채취 ·
  제4항 기준 **0.03%** · **제5항 음주측정방해행위**(추가 음주 등, 2024.12.3. 신설) · 제6항 방법·절차는 행정안전부령.
* **시행규칙 제27조의2** — 여기에 실제 절차가 있다.
  호흡조사: ① 「교통의 안전과 위험방지를 위하여 필요」하거나 **운전자의 외관·언행·태도·운전 행태 등 객관적 사정**으로 의심될 때
  ② **입 안의 잔류 알코올을 헹궈낼 수 있도록 음용수를 제공**할 것.
  혈액 채취: ① 처음부터 요구 / 호흡조사 불복 + 동의 / 의식 없음 등 호흡 불가 ② 의료기관에서 **비알콜성 소독약** ③ **국립과학수사연구원 등** 감정 의뢰.
  제3항이 「그 밖의 사항은 **경찰청장이 정한다**」인데 그 지침은 국가법령정보센터에 없다 → 넣지 않고 「미확인」에 적었다.
* **제148조의2** — 0.03~0.08 1년 이하/500만원 이하 · 0.08~0.2 1~2년/500만~1천만 · 0.2 이상 2~5년/1천만~2천만 ·
  **측정거부** 제2항 제1호 1~5년/500만~2천만 · **측정방해** 제2항 제2호 같은 형 · **10년 내 재범** 제1항 가중.
* **시행규칙 별표28** — 0.03~0.08 **벌점 100(정지)** · 0.08 이상 **취소** · 측정거부 **취소**.
* **자전거등**(시행령 별표8) — 음주운전 PM 10만 / 자전거 3만 · 측정불응 PM 13만 / 자전거 10만 · 측정방해 같은 금액.
* **약물** 제45조 → 제148조의2 제5항 5년 이하/2천만 이하, 측정불응 제6항(2025.4.1. 신설).
* **「음주감지기」는 법령에 없다.** 실무 장비다 — 조문을 붙이지 않고 화면에 「법령에 근거 조문 없음 — 실무 장비」로 적었다.

### 게임
* `js/drunkproc.js` 신설 · `data/laws.json` 에 **`drunkProc`** 블록 추가. **수치·조문은 코드에 한 줄도 없다** —
  파일을 비우면 절차 화면이 아예 열리지 않는다(검증이 그것을 확인한다).
* 음주 의심으로 세워 고지가 끝나면 절차 화면이 뜬다: **① 의심 → ② 감지 → ③ 음용수 → ④ 측정 → ⑤ 고지 → ⑥ 채혈**.
  **순서를 어기면 −5 와 함께 왜 안 되는지 조문으로 말해 준다** — 음용수 없이 측정하려 하면
  「입 안의 잔류 알코올을 헹궈야 합니다(시행규칙 제27조의2 제2항 제1호 나목)」.
* 갈래: 감지기 무반응 → 음주 아님 · 측정 거부 → 제148조의2 제2항 제1호 + 취소 · 결과 불복 → 동의 채혈 · 기준 미만 → 처리 안 함.
* **구현과 현실의 차이를 화면에 적었다** — 「측정값은 연습용 임의값이고 실제 단속 통계가 아니다」,
  그리고 **1차 출처를 못 찾아 넣지 않은 것 3가지**(감지 후 대기시간 · 결과지 서명·교부 · 위드마크)를 그대로 나열한다.
  숨기면 게임을 실무로 착각하게 된다.
* 검증: verify **ERRORS(0)** · 음주 자료(기준 0.03 · 단계 6 · 등급 3구간 · 거부/방해/재범 · 자전거·PM 3행 · 미확인 3건) ·
  절차(자료 없으면 안 열림 · 순서 위반 1회 감지 · 등급·처벌·취소 표시 · 거부 갈래 · 무반응 갈래 · **괴리 고지** · 일시정지 해제) ·
  겹침 0.68m · 교차로 근무 2초 이동 0m · 신호등 90/90 · 보행 신호 짧은 곳 0 · 도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.31 — 화면 점검(소유자: 「모든 화면 확인 점검한다 천천히 정확하게」)
브라우저에서 모드를 하나씩 열어 보고 눈으로 찾은 것만 고쳤다. 짐작으로 고치지 않았다 — 의심되면 DOM·좌표로 확인했다.

* **모드를 바꿔도 앞 모드의 안내가 남았다.** 추격전 힌트가 순찰 근무 화면 위에 그대로 떠 있었다.
  `hud.hint('')` 은 `hintGap` 에 막히고 빈 글씨로 남을 뿐이라 **`hud.clearHint()`** 를 따로 만들어 `start()` 에서 부른다.
* **교차로 근무에서 제어함이 안 보였다.** 경찰관이 함체를 **등지고** 서서 몸으로 가렸다.
  함체 **옆**(교차로–함체 축의 직각으로 1.7m)에 서서 **교차로를 보게** 바꿨다 — 근무 자세로도 맞다.
* **목표 빔이 제어함을 덮었다.** 노란 기둥은 **찾을 때** 쓰는 것이라, 6m 안에서는 끄고 바닥 고리만 남긴다(`markerFade`).
* **음주 절차 카드 글씨가 가운데로 몰렸다** — `.card` 가 가운데 정렬이라 조문·본문이 읽기 어려웠다. 왼쪽 정렬로 고정.
* 확인했으나 **문제가 아니었던 것**(짐작으로 고치지 않았다):
  순찰 시작 위치는 차로 중앙(lateral 2.0 = 차로중심 2.0)이 맞았다 · 「꼬리물기」 표기 정상 · 조작 단추 화면 밖 잘림 없음 ·
  제어함 모델 자체는 기초·함체·남색 캡·시건장치까지 갖춰져 있었다.
* 아직 안 고친 것(다음 판): 세로 화면에서 인트로 구도가 한쪽으로 몰린다 · 미니맵 도로 이름이 서로 겹친다 ·
  교차로 근무를 **하차 근무**로(차로 도착 → 경광등 → 하차 → 제어함).
* 검증: verify **ERRORS(0)** · 교차로 근무(2초 이동 0m · 제어함 1.7m · **화면 안** · 가까이서 빔 꺼짐) ·
  음주 절차 전 갈래 · 겹침 0.68m · 신호등 90/90 · 보행 신호 짧은 곳 0 · 도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.32 — 횡단보도 제자리걸음 · 「건너는 중」 무한 · 제어기 화면의 틀린 숫자
소유자: 「수동으로 제어기를 조작하는 것도 나와야 하고 **횡단보도위 사람들이 같은 자리만 걷고 있어** 버그」

### ① 제자리걸음 — **내가 만든 버그였다**(v0.9.29 겹침 풀기)
겹침을 풀 때 **진행 방향으로도 밀었다.** 같은 방향으로 줄지어 걸으면 뒤 사람이 매 프레임 뒤로 밀려
실속력이 **0.19m/s**(설정 1.98)까지 떨어졌다 — 걷는 시늉만 하고 나아가지 않았다.
**옆으로만 벌리도록** 고쳤다(각자의 진행 방향에 직각인 성분만). 사람은 나란히 서지, 서로를 뒤로 밀지 않는다.
완전히 일직선이면 정해진 쪽으로 비킨다.

### ② 「건너는 중」에서 못 빠져나옴 — 원래 있던 버그
`nodeAhead` 는 교차로를 지나치면 **다음** 교차로를 돌려준다. 그것으로 「다 건넜나」를 재니 거리가 다시 커져
상태가 끝나지 않았다 — **88초·134m 를 횡단 중**인 보행자를 확인했다.
건너기 시작할 때 **`p.crossNode` 를 기억**해 그 교차로 기준으로 판정하도록 고쳤다.
앞에 교차로가 아예 없어진 경우도 걸어가는 상태로 되돌린다.
지금은 관찰 14~17명 · **제자리걸음 0 · 45초 초과 0 · 최장 29~31초**(8차로 37m 에 맞는 시간).

### ③ 제어기 화면이 틀린 숫자를 보였다
「보행 최소 **20초**」는 `cfg.PED_WALK`(하한)였다. v0.9.28 부터 보행 시간은 **도로 폭으로** 정해진다(이 교차로 25~37.2초).
* 전환(황색·전적색) 중에는 녹색 축이 없어 하한으로 떨어졌다 → **두 횡단보도 중 긴 쪽**을 보이게 했다.
* 수동 전환 안내도 `PED_WALK` 대신 그 교차로의 실제 값으로 바꿨다.
* 수동 유지 판정의 `needMin` 도 `PED_WALK + 2` 가 아니라 **`greenMin(node, axis)`** 로 바꿨다.
* 이미 그 방향이 녹색이면 요청(`c.req`)을 지운다 — 안 지우면 「유지 중」으로 안 보였다.

### 확인만 하고 안 고친 것
**수동 제어기 조작 화면은 이미 있었다.** 소유자가 못 찾은 이유는 v0.9.31 에서 고친 **제어함 가림**이었다.
브라우저에서 직접 눌러 확인했다 — 「자동 운영 / 수동 조작」, 방향별 대기 행렬, 「남북·동서 녹색」 요청,
「바깥 차로 차단」, 「꼬리 끊기」가 다 동작하고 경찰관도 조작 자세를 취한다.
`manual holds green` 검사가 실패로 나온 것도 **코드가 아니라 검사식**이 틀린 것이었다(앞 검사가 전환 중 상태를 남겨
축을 잘못 골랐다). 브라우저에서 재 보니 elapsed 가 30에서 멈추고 유지된다 —
검사를 「유지될 때까지 돌려 보고 안 되면 실패」로 바꿨다(42초 만에 유지 확인).

* 검증: verify **ERRORS(0)** · 횡단 보행자(제자리 0 · 45초 초과 0 · 최장 29초) · 제어기 보행 시간 어긋난 교차로 0 ·
  수동 녹색 연장 확인 · 음주 절차 전 갈래 · 겹침 0.68m · 교차로 근무 제어함 화면 안 · 신호등 90/90 ·
  도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.33 — 하차 근무 · 세로 화면 화각 · 미니맵 이름표 겹침
소유자: 「전부 다하자」. 밀린 세 가지 중 화면·조작 쪽을 먼저 묶었다.

* **교차로 근무를 하차 근무로 바꿨다**(소유자: 「도보근무로 바꿔주는데 왠지 하차근무가 좋을지 싶네」).
  전에는 **이미 내려 있는 상태**로 시작해 근무의 앞부분이 통째로 없었다.
  이제 접근로 **100m 뒤에서 순찰차로 시작**한다 — 경광등을 켠 채 갓길까지 가서 세우고 **「하차」**로 내린다.
  * `onFoot()` 에서 duty 를 뺐다. 내리기 전까지는 「차 안」이고 `G.afoot` 이 켜지면 도보다(main.js·enforcement.js 둘 다).
  * **교차로에서 70m 넘게 떨어져 내리려 하면 거부**한다 — 멀리 내리면 걸어갈 수 없다.
  * 차 안에서도 그 교차로는 돌아간다 — 대기 행렬·꼬리물기를 계속 세고 구간 줄에 보여 준다.
  * `walker` 가 없는 동안을 다 막았다(`dutyOpen`·`raiseHand`·`toggleBox`·`dutyRules`).
  * 하차 안내가 `hintGap` 에 막혀 「제어함으로」가 안 보였다 → `hintNow` 로.
* **세로 화면(폰)에서 인트로 구도가 한쪽으로 몰렸다.** three.js 의 `fov` 는 **세로** 화각이라,
  세로로 긴 화면에서는 가로 화각이 확 좁아진다 — 도시가 오른쪽 위 구석에 몰리고 아래 절반이 빈 들판이었다.
  `fovFit(f, aspect)` 로 **가로 화각을 16:9 기준으로 유지**하도록 세로 화각을 키운다(상한 105도).
  420×900 에서 62 → 105, 정사각에서 62 → 93.8.
* **미니맵 이름표가 서로 겹쳐 못 읽었다.** 먼저 그린 것이 이기고 겹치는 것은 건너뛴다.
  **중요한 순서**로 그린다: 도로명 → 다리 → 지하철역 → 랜드마크 → 자연. 달리는 사람에게 가장 쓸모 있는 것이 도로명이다.
  지금 15개를 그리고 **겹침 0**, 21개를 건너뛴다. 도로명 6개는 하나도 안 빠진다.
* 검증: verify **ERRORS(0)** · 하차 근무(차 안 시작 · 110m · 멀리서 거부 · 가까이서 하차 · 제어함 목표) ·
  세로 화각 · 미니맵 겹침 0 · 화면 교차로 근무/하차/승차 · 도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.34 — 어린이 안전띠 + 교실을 여섯 단계로
소유자: 「어린이 안전띠도 했는가 싶다」 · 「횡단보도를 건널때 만을 말하고 있는데 전반적인 교통안전을 말하는것이였고」

### 안전띠 — 게임에 **아예 없었다**
`data/laws.json` 에 **`carSafe`** 를 새로 넣었다. 전부 원문 대조본이다(2026-09-10).
* **도로교통법 제50조 제1항** — 운전자도 매고 **모든 좌석의 동승자**에게도 매도록 하여야 한다.
  영유아는 「**유아보호용 장구를 장착한 후의** 좌석안전띠」다 — 카시트 없이 안전띠만 매는 것은 이 조문을 지킨 것이 아니다.
* **낱말이 다르다**: 「영유아」는 **6세 미만**(제11조 제1항), 「어린이」는 **13세 미만**(제2조 제23호).
* **과태료**(제160조 제2항 제2호 · 시행령 별표6 제9호) — 동승자가 **13세 미만 6만원 · 13세 이상 3만원**.
  **운전자가 낸다.** 어린이를 태우면 두 배다.
* 어린이통학버스(제53조 제2항 · 별표6 제11호의3) **6만원**.
* 내릴 때는 인도 쪽 문(제39조 제3항 · 교특법 단서 제10호).
* **운전자 본인 미착용 범칙금은 「확인 중」으로 뒀다** — 시행령 별표8 표가 고정폭 CDATA 라 행과 금액이 떨어져 있어
  자동으로 못 읽었다. 지어내지 않는다.

### 어린이 교실 — 횡단보도 하나 → 여섯 단계
`js/kidcourse.js` 신설. **무엇을 가르칠지는 서초구 실제 자료가 정했다**(v0.9.27 의 `taas-vuln-seocho.json`):
어린이 사고 395건에서 **횡단중은 40건뿐**이고 측면충돌 134 · 추돌 54 가 더 많다 → **차에 타고 있을 때**도 다친다.
차도통행중 11 · 길가장자리 5 · 보도통행중 5 → 걸을 자리. 자전거 사고 중 **보도통행중 37건**.
보행자 사고 가해차종에 **이륜 117 · 자전거 98 · PM 72** → 위험한 것이 자동차만은 아니다.

① 🚶 걸을 자리(보도로 8초) → ② 🚦 건널 자리(현행 4단계) → ③ 🚗 차에 탈 때(순찰차에 가까이 가면)
→ ④ 🚲 자전거 → ⑤ 🛴 킥보드(지나가는 것을 보면) → ⑥ 🌳 서로 조심(앞 다섯을 다 배우면 자동으로 열린다).
단계마다 +15, 결과 카드에 배지로 남는다. 학습 화면에도 여섯 단계 표가 붙는다.

* **함정 하나**: `G.kidCourse.start()` 를 `G.mode` 가 정해지기 **전에** 불러 늘 꺼져 있었다.
  `start()` 안에서 순서가 「enforcement 생성 → … → G.mode 설정」이라는 것을 검증이 잡아냈다(켜짐 false).
* 검증: verify **ERRORS(0)** · 어린이 교실(켜짐 · 단계 6 · 다섯 뒤 5 · 여섯 뒤 6 · 배지 6 · HUD 줄) ·
  안전띠 자료(6항목 · 제160조 제2항 제2호 · 6만/3만 · 미확인 1) · 학습 화면에 안전띠 단·교실 표·유아보호용 장구 ·
  도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.35 — 얼굴 정리 · 안내문이 4단계 칩을 덮던 것
소유자: 「게임상 사람들 어린이 얼굴을 좀더 세련되게 표현해줘 봐서 이상하지 않게 너의 의견은 어떤지」

**사실적으로 가지 않았다.** 디테일을 더하면 불쾌한 골짜기로 들어가 어린이 홍보물로 쓰기 더 어려워진다.
세련되게 만드는 길은 **더하는 것이 아니라 정리하는 것**이라고 판단하고 그렇게 했다.
* **코를 그리지 않는다** — 상자 코가 얼굴 한가운데 혹처럼 보였다.
* **눈은 흰자를 없앴다** — 흰자와 눈동자가 같이 있으면 크기가 조금만 어긋나도 어색해진다.
  짙은 눈 하나 + 하이라이트 한 점. 상자 하나면 네모로 보여 **좁게·넓게·좁게 세 겹**으로 쌓아 둥글게 했다(면 수는 그대로 적다).
* **입은 선 하나** — 입술·입안·입꼬리를 따로 그리지 않는다. 표정은 곡률(scale)로 만든다. 어린이만 입꼬리를 살짝.
* **볼 홍조는 어린이만** — 어른에게는 어색하다.
* 대신 **머리 실루엣 네 갈래**(짧은머리·단발·가르마·묶은머리)를 넣었다 —
  사람은 얼굴보다 실루엣으로 구별된다. 경찰관은 정모를 쓰므로 짧은머리 고정.
* 깜빡임·말하기·웃음은 그대로 동작한다(검증이 확인한다).

**안내문이 어린이 4단계 칩을 덮었다.** 좁은 화면에서 안내문이 두세 줄이 되어 칩 위로 올라앉았다.
`hud.notice` 가 안내문 높이만큼 칩을 아래로 내리고, 안내문이 사라지면 되돌린다(`pushKidChips`).

* 검증: verify **ERRORS(0)** · 얼굴(눈 2 · 입 1 · 웃음·말하기 동작 · 머리 실루엣 4갈래 면 수 1698/1746/1722/1839) ·
  어린이 교실 6단계 · 안전띠 자료 · 하차 근무 · 세로 화각 · 미니맵 겹침 0 · 도로 연결 12,545/0 · 3분 소크 NaN 0.

### 밀린 여섯 가지를 v0.9.33~35 로 모두 끝냈다
① 하차 근무 ② 세로 화면 인트로 구도 ③ 미니맵 이름표 겹침 ④ 어린이 안전띠 ⑤ 교실 확장 ⑥ 얼굴 정리.

## v0.9.36 — 실제 지도 1단계: **격자를 지도 파일이 정한다**
소유자: 「실제 서초구 지도를 진행하자 1단계부터」

* 격자는 `city.js` **두 곳**에서만 들어왔다(`cfg.ROAD_XS/ZS`, `cfg.LANES_V/H`) — 깨끗한 이음매였다.
  이제 `data/maps/<id>.json` 의 **`grid`**(`xs`·`zs`·`lanesV`·`lanesH`)가 정한다.
  **도로 개수도 지도가 정한다** — 5×5 가 아니어도 된다.
* `seocho.json` 에는 **지금과 똑같은 값**을 적었다 → 화면 변화 0(검증이 「현재값 = 옛 config 값」을 확인한다).
* 형식이 틀리면 **config.js 로 물러선다**. 파일을 못 읽어도 게임은 돈다는 규칙 그대로다.
* **격자를 바꾸면 지도 파일의 다른 항목이 격자 밖을 가리킨다** — 3×4 로 시험하다가 지하철역에서 죽었다.
  `inNode`·`inBlock`·`clampBlock` 으로 넘치는 역·랜드마크·블록을 조용히 건너뛴다.
  지도 하나가 잘못됐다고 게임이 죽으면 안 된다.
* **미니맵 도로 이름표도 격자에서 뽑는다** — 자리를 코드에 적지 않는다. 차로가 많은(간선) 도로부터 적어
  좁은 미니맵에서 중요한 것이 남는다. 지도가 바뀌면 이름표도 따라 움직인다.
* 아직 옮기지 않은 것(2단계): 순환도로·연결로의 모양, 한강·철길 위치, **일방통행·회전차로·비스듬한 교차로**.
* 검증: verify **ERRORS(0)** · 격자 출처 `map` · 파일값 = 현재값 · 현재값 = 옛 config 값(회귀 0) ·
  **3×4 로 갈아끼워도 실제로 지어짐**(차로 수·반폭 반영) · 형식 틀리면 물러섬 ·
  미니맵 도로명 남북 5/5 · 겹침 0 · 도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.37 — 두 번째 지도: **서울 서초구 (실측 간격)** — 골라 쓴다
소유자: 「맵은 기존맵은 두고 서초구 맵을 만드는것이니까 선택할수 있게 해줘」

* **베타 지도는 그대로 두고** `data/maps/seocho-real.json` 을 새로 만들었다. 교통시설 화면의 「🗺 지도」에서 고른다.
  `?map=seocho-real` 로도 열린다.
* **도로 간격이 실측값이다.** OpenStreetMap(ODbL) Overpass 에서 서초구 간선 10개를 받아,
  격자가 실제로 만나는 구역(위도 37.478~37.510 · 경도 126.980~127.030) 안의 way 중심점 **중앙값**을
  도로마다 하나씩 뽑고 이 지도의 아핀변환으로 옮겼다.
  `xs = [22.2, 80.1, 147.6, 212.6, 312.0]` · `zs = [22.0, 71.2, 156.3, 282.5, 364.6]`
  간격이 균일 80 이 아니라 **57.9/67.5/65.0/99.4** 와 **49.2/85.1/126.2/82.1** 로 제각각이다.
* **차로 수는 OSM 에서 가져오지 않았다.** `lanes` 태그가 편도/왕복이 섞여 있어 반포대로가 2로, 방배로·서초중앙로가
  빈 값으로 나왔다 — 믿을 수 없어서 베타와 같은 값(소유자·T-Book 근거)을 쓰고 **파일에 그렇게 적었다**.
  간격만 실측이라는 것을 숨기지 않는다.
* **순환도로 중심을 도시 격자 한가운데로** 바꿨다(`CXC/CZC`). 160,160 에 못 박아 두면 실지도에서 도시가 고리 한쪽으로 치우친다.
  베타 지도는 값이 같아 변화가 없다.
* **미니맵 도로 이름표를 격자 바깥 여백으로 뺐다.** 격자 안에 놓으면 세로 이름표(글자 길이만큼 긴 상자)가
  가로 이름표를 다 밀어냈다 — 세 번 고쳐 보고서야 자리가 문제라는 걸 알았다. 지금은 열 개가 거의 다 남는다.
* 검증(**두 지도 각각**): verify **ERRORS(0)** · 격자 출처 `map` · 베타는 옛 config 값과 일치(회귀 0) ·
  도로 연결 **베타 12,545 / 실측 12,556, 끊김 둘 다 0** · 미니맵 겹침 0, 빠진 도로명 없음 · 3분 소크 NaN 0.

## v0.9.38 — 도로 위의 나무 · 하차 버튼 · 후진 버튼 · 음주 단속 항목
소유자가 달려 보고 신고한 네 가지. 세 개는 진짜 버그였고 하나는 「되는데 찾을 수 없었다」였다.

* **도로 위에 나무가 서 있었다.** 연결로 가로수를 「그 도로의 가장자리」로만 계산해 심다 보니
  그 자리가 **다른 도로(순환도로·램프) 한가운데**인 경우가 있었다.
  이제 어디에 심든 `treeOK(x, z)` 를 지난다 — 도로 반폭 + 3.5m 안이거나 도시 격자 포장이면 심지 않는다.
  1,434 그루를 심고 **18 그루를 걸러 냈다**. 검증이 포장 표본 205곳에서 허용된 자리 0 을 확인한다.
* **하차 버튼이 없었다.** 두 가지가 겹쳤다 — `can-foot` 에 duty 가 빠져 있었고, CSS 가 `dutymode` 에서 버튼을 숨겼다.
  v0.9.33 에서 교차로 근무를 하차 근무로 바꿔 놓고 **내릴 버튼을 막아 둔 셈**이었다.
* **후진 버튼이 없었다.** 후진 자체는 됐다(스틱을 아래로 2.7초 유지하면 기어 R, 16km/h — 브라우저에서 직접 쟀다).
  다만 그 방법은 찾을 수가 없다. **누르는 동안 바로 뒤로 가는 「🔙 후진」 버튼**을 넣었다(R 키와 같다).
* **음주 의심 차량이 단속 보기에 안 떴다.** 「비틀거리는 차면 음주를 보기에 넣는다」를 넣었는데
  **바로 아래 무작위 보기가 그 자리를 덮어썼다** — `else if` 사슬이라 앞에서 넣어도 뒤가 다시 채운다.
  `forced` 로 막았다. 이제 사행 차든 기록된 차든 「음주운전 의심」이 항상 보기에 있다.
* 검증: verify **ERRORS(0)** · 나무(심음 1,434 · 건너뜀 18 · 포장 위 허용 0) ·
  후진 버튼(보임 · 누르면 R 13km/h) · 음주 항목(사행 차·기록된 차 둘 다 보임) ·
  하차 근무 · 격자 출처 map · 도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.39 — 정지선 앞 4색 신호등 · 보행등 크게 · 인트로에는 무단횡단 없음
* **한국 교차로는 신호등이 앞뒤로 있다**(소유자: 「신호등이 교차로 건너기전에 4색신호등이 있고 그 넘어에도 있어」).
  지금까지는 **건너편 것만** 있었다 — 접근로에 달린 머리는 반대편 운전자가 읽는 것이라,
  정작 정지선에 선 운전자에게는 **뒷면만** 보였다. 같은 팔에 **뒤를 보는 머리**를 하나 더 달았다(90개).
  검증이 「건너편 90 / 정지선 앞 90 / 짝 어긋남 0」을 확인한다.
* **보행등을 키웠다**(소유자: 「보행자신호등도 잘 보이게 배치해주고」).
  판 0.5×1.0 → **0.66×1.32**, 함체 0.56×1.08 → 0.74×1.40, 높이 2.75 → **2.95m**, 차양도 함께 키웠다.
  길 건너에서 읽어야 하는 물건이라 작으면 소용이 없다.
* **인트로에서는 무단횡단이 나오지 않는다**(소유자: 「좋은세상을 보여줘야지 너무 무질서 하네」).
  홍보 영상에 무질서를 담을 이유가 없다. `peds.noJaywalk` 로 인트로 동안 성향 자체를 끄고,
  이미 걷고 있던 사람의 성향도 지운다. **인트로가 끝나면 되돌린다** — 순찰 근무에는 무단횡단이 있어야 단속을 배운다.
  검증: 40초 돌려 46명 중 성향 0 · 진행 중 0 · 끝난 뒤 복구 확인.
* 검증: verify **ERRORS(0)** · 신호등 앞뒤 90/90 짝 0 · 접근로 90 = 차량등 90(보조·정지선 앞은 제외하고 셈) ·
  보행등 360/뒤판 180/보조 90 · 인트로 무단횡단 0 · 도로 연결 12,545/0 · 3분 소크 NaN 0.

## v0.9.40 — 근무 일지 + 오답 노트 (내 판단으로 메운 두 구멍)
소유자: 「게임개발 전문가인 니가 봤을때 고쳐야 할것들을 고쳐라」

먼저 **새 결함이 있는지 실측**했다 — 3분 소크에서 오류 0, 업데이트 p50 1.0ms / p99 1.9ms / 최대 5.9ms,
장면 메시 1,300개(드로콜 296). 성능도 안정성도 지금은 문제가 아니었다.
그래서 v0.9.31 에서 내가 스스로 지적했던 **구조적 구멍 두 개**를 메웠다.

### ① 한 판이 끝나면 아무것도 남지 않았다 → 근무 일지
`js/career.js` 신설. localStorage `tg_career` 에 **근무 횟수 · 최고 점수 · 누적 점수 · 단속 수 ·
정답/오답 · 모드별 횟수 · 연속 근무일**을 쌓는다. 타이틀 화면과 결과 카드에 한 줄로 보인다.
「근무 12회 · 정답률 78% · 최고 255점 · 연속 3일 · 다시 볼 것 4개」

### ② 틀린 문제가 틀린 채로 지나갔다 → 오답 노트
localStorage `tg_wrong`. 단속 퀴즈에서 **틀리면 그 항목이 노트에 쌓이고, 두 번 더 맞혀야 빠진다**(최대 3).
* 결과 카드에 「📕 다시 볼 것 · 음주운전 의심(3번 더) · 보행자 보호의무 위반(2번 더) … — 학습 화면에서 다시 보세요」
* 학습 화면에 **「📕 다시 볼 것」** 단 — 틀린 횟수 많은 순으로, 각 항목의 조문·현장 요령과 함께.
* 학습은 반복에서 온다. 한 번 틀린 것을 한 번 보고 넘기면 남지 않는다.

### 지킨 것
* **서버 0대·네트워크 요청 0** 그대로. 전부 이 기기 localStorage 이고 키는 `tg_` 접두사다.
  검증이 `tg_career`·`tg_wrong` 두 키만 생기고 **`tb_` 키는 하나도 안 건드리는 것**을 확인한다.
* 법령 문구는 여전히 `laws.json` 에서만 읽는다 — 오답 노트도 조문을 직접 적지 않는다.
* `G.career` 는 **init 에서** 만든다. `start()` 안에서 만들었더니 첫 실행 타이틀에 일지가 안 떴다.
* 검증: verify **ERRORS(0)** · 오답 노트(틀린 뒤 2 → 한 번 맞혀 1 → 두 번째에 빠짐 · 많이 틀린 것이 앞) ·
  근무 일지(근무 2 · 최고 120 · 누적 200 · 정답률 · 저장 키 2개 · **tb_ 침범 0**) · 학습 화면 두 단 ·
  신호등 앞뒤 90/90 · 인트로 무단횡단 0 · 도로 연결 12,545/0 · 3분 소크 NaN 0.

### 손대지 않은 것과 그 이유
* **성능 최적화** — 신호등 머리가 630개(차량 90+90, 보조 90, 보행 360)라 메시가 많지만,
  드로콜 296 · 업데이트 p99 1.9ms 로 아직 여유가 있다. 실기(폰)에서 느리다는 보고가 오기 전에는 건드리지 않는다.
* **차내 시점 완성도** — 소유자가 지적했지만 화면을 크게 바꾸는 일이라 소유자와 방향을 맞춘 뒤에 한다.

## v0.9.41 — 보행등 위 4색등 삭제 · 어린이 걸음으로 건널 수 있는 보행 시간
소유자: 「보행자 신호기 바로위에 있는 4색 신호등을 삭제해주고」 · 「어린이 걸음으로 충분히 걸을수 있도록하자」

### ① 보행등 위 4색등을 지웠다 — **내가 만든 것이었다**
v0.9.29 에서 「횡단하는 사람 눈에 4색 신호가 안 보인다」를 풀려고 보행등 기둥 4.30m 에 4색등을 달았다.
**한국 도로에 그런 배치는 없다**(소유자 지적). 지웠다(교차로당 4개 × 25 = 90개 제거).
그 문제는 v0.9.39 의 **정지선 앞 4색등**이 이미 제대로 풀었다 — 없는 시설을 지어내지 말았어야 했다.

### ② 보행 시간을 어린이 걸음에 맞췄다
* **횡단 거리를 잘못 재고 있었다.** `2 × sideOff`(보도 바깥선까지)를 썼는데, 사람은 보도 위를 「건너지」 않는다.
  **`2 × halfOf`(연석에서 연석까지, 차도 폭)** 로 고쳤다 — 양쪽 보도 6m 만큼 과하게 잡혀 있었다.
* 설계값을 `config.js` 로 뺐다: **진입 7초 · 보행속도 1.0m/s · 어린이보호구역 0.8m/s**.
  **법령 수치가 아니다** — 도로교통법·시행규칙에 보행 신호의 초 단위 기준이 없고,
  경찰청 「교통신호기 설치·관리 매뉴얼」은 국가법령정보센터에 공개돼 있지 않아 대조하지 못했다. 파일에 그렇게 적었다.
* `greenMin` 이 5초·1.15m/s 를 **따로 적어 두고 있었다** — pedTime 을 고쳐도 따라오지 않는 자리였다. 한 곳으로 합쳤다.
* **읽는 순간 하한을 강제한다**(`gvOf`/`ghOf`). 교통시설 화면이나 이전 판이 저장한 짧은 값이 들어와도
  「보고 출발했는데 다 건너기 전에 적색」이 다시 생기지 않는다. `phase`·`cycleOf`·`pedRemain` 도 같은 값을 쓴다.
* **다 건널 때까지 기다려 준다.** 아직 횡단보도 위에 사람이 있으면 초록불을 붙잡는다(`holdPed`,
  한 신호에 최대 12초). 실제 스마트 횡단보도와 같은 생각이다. 연장 중에는 「뛰어요」가 아니라
  **「다 건널 때까지 기다려 줘요 — 뛰지 말고 걸어요」**가 뜬다.
* 결과: 보행 시간 **27~41초**, 어린이 걸음(1.25m/s)으로 **최소 여유 8.6초**, 녹색이 보행을 못 품는 곳 **0**,
  주기 88~95초(대형 교차로로는 실제와 비슷하지만 **차량 대기가 길어진다** — 소유자 확인이 필요한 맞바꿈).
* 검증: verify **ERRORS(0)** · 보행등 위 4색등 0 · 신호등 앞뒤 90/90 · 보행 신호 짧은 곳 0 ·
  어린이 여유 8.6초 · 연장 12.5초 유지·한도 초과 0 · 제어기 표시 일치 · 도로 연결 12,545/0 · 3분 소크 NaN 0.
