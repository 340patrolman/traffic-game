# reference clones — never part of the deliverable
ref/
.DS_Store
Thumbs.db
_verify*
_shot*
_ic*
_probe*
_bike*
# 로컬 전용 실측 자료(현시도 등) — 공개 여부가 정해지기 전에는 저장소에 올리지 않는다
data/local/
_audit*
# 세션 부산물(스크린샷 등) — 배포물이 아니다
Claude outputs/
# 가공 중간 파일(원본 CSV 는 올린다 — 재현을 위해)
data/raw/_*.csv
