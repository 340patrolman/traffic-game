// 경찰 표장 원본 이미지(데이터 URI). 소유자가 제공한 실물 마크를 그대로 쓰기 위한 자리다.
//
//  · shield : 방패형 「경찰 POLICE」 마크(남색 바탕) — 순찰차 도어·HUD·타이틀
//  · eagle  : 참수리 표장(바탕 없음) — 신호제어기·정모·홍보물
//
// 값이 null 이면 textures.js 가 캔버스로 그린 마크를 쓴다(지금 상태).
// 채우는 방법: 원본 PNG 를 base64 로 바꿔 'data:image/png;base64,...' 문자열로 넣는다.
// 파일을 따로 두지 않고 문자열로 넣는 이유는 「외부 이미지 파일 0개 · 네트워크 요청 0」 규칙을 지키기 위해서다
// (오프라인·file:// 에서도 그대로 뜬다).
window.TG = window.TG || {};
TG.EMBLEM = { shield: null, eagle: null };
